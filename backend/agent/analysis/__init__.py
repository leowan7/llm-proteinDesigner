"""Analysis subpackage for post-run candidate evaluation.

Provides:
    cache    — In-memory candidate cache keyed by job_id
    ranking  — Pandas-based ranking, filtering, and distribution stats
    tools    — Tool handler functions for load_job_results, analyze_candidates,
               flag_red_flags
"""
