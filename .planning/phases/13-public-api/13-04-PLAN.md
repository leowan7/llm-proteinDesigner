---
phase: 13-public-api
plan: 04
type: execute
wave: 3
depends_on:
  - 13-02
  - 13-03
files_modified:
  - backend/api/v1/api_keys.py
  - backend/api/v1/__init__.py
  - backend/user/api_keys.py
  - backend/user/router.py
  - bindwave-python/src/bindwave/_client.py
  - bindwave-python/src/bindwave/jobs.py
  - bindwave-python/src/bindwave/api_keys.py
  - bindwave-python/src/bindwave/_exceptions.py
  - bindwave-python/src/bindwave/types/__init__.py
  - bindwave-python/src/bindwave/types/job.py
  - bindwave-python/src/bindwave/types/api_key.py
  - bindwave-python/src/bindwave/__init__.py
  - bindwave-python/tests/test_client.py
  - bindwave-python/tests/test_jobs.py
  - bindwave-python/tests/test_api_keys.py
  - bindwave-python/examples/submit_and_wait.py
  - bindwave-python/README.md
  - backend/tests/api_v1/test_api_keys.py
  - backend/tests/contract/_openapi_paths_snapshot.txt
autonomous: true
requirements:
  - API-01
  - API-03
  - API-12
must_haves:
  truths:
    - "/api/v1/api-keys GET returns {data: [...]} list of caller's org keys (revoked excluded)"
    - "/api/v1/api-keys/{key_id}/revoke POST flips revoked_at and returns 404 cross-org"
    - "/user/api-keys GET returns array of caller's org keys (web auth via require_role)"
    - "/user/api-keys POST creates a key and returns {plaintext, prefix, id, name, created_at} — plaintext shown EXACTLY ONCE; never on GET"
    - "/user/api-keys/{key_id}/revoke POST sets revoked_at; subsequent calls 404"
    - "/user/api-keys routes are EXPLICITLY hidden from the OpenAPI spec because the new APIRouter is constructed with include_in_schema=False directly (FastAPI does NOT inherit the flag from a parent router); D-15 contract test in this plan asserts /user/api-keys does not leak into /api/openapi.json"
    - "bindwave.Client(api_key='bw_test_x').jobs.submit(tool='rfdiffusion', parameters={...}) returns a Job with id + status"
    - "bindwave.Client(api_key=...).jobs.get(id) / list() / cancel(id) work against the v1 surface (respx-mocked)"
    - "bindwave.Client(api_key=...).api_keys.list() / revoke(id) work (respx-mocked)"
    - "Idempotency-Key header is auto-generated as uuid4().hex when caller does not supply"
    - "Authorization: Bearer header is sent on every request; NO X-Org-Id header sent"
    - "5xx and 429 responses trigger exponential backoff retry capped at 5 attempts; Retry-After is honored on 429"
    - "application/problem+json error parsing routes to typed exception class hierarchy: BindwaveAuthError (401), BindwaveRateLimitError (429), BindwaveValidationError (400+422), BindwaveJobError (other 4xx), BindwaveAPIError (5xx)"
  artifacts:
    - path: "backend/api/v1/api_keys.py"
      provides: "GET /, POST /{key_id}/revoke endpoints (SDK self-management)"
    - path: "backend/user/api_keys.py"
      provides: "GET /, POST /, POST /{key_id}/revoke endpoints (web flow for SettingsPage)"
    - path: "bindwave-python/src/bindwave/_client.py"
      provides: "Client class (sync) with jobs + api_keys resource attributes"
    - path: "bindwave-python/src/bindwave/jobs.py"
      provides: "JobsResource: submit, get, list, cancel methods"
    - path: "bindwave-python/src/bindwave/api_keys.py"
      provides: "ApiKeysResource: list, revoke methods"
    - path: "bindwave-python/src/bindwave/_exceptions.py"
      provides: "BindwaveError hierarchy + parse_error_response helper"
  key_links:
    - from: "backend/user/api_keys.py POST /"
      to: "backend/auth/api_keys.generate_api_key"
      via: "generate_api_key(env='live') returns (plaintext, prefix, hash)"
      pattern: "generate_api_key\\("
    - from: "backend/user/api_keys.py POST /"
      to: "api_keys table"
      via: "INSERT INTO public.api_keys ... RETURNING id, created_at"
      pattern: "INSERT INTO public.api_keys"
    - from: "bindwave._client.Client"
      to: "backend /api/v1 surface"
      via: "httpx.Client with Authorization: Bearer header + base_url"
      pattern: 'Authorization":\s*"Bearer'
    - from: "bindwave._exceptions"
      to: "RFC 7807 problem+json bodies"
      via: "parse_error_response reads type URL slug + maps to exception class"
      pattern: 'problem\\+json'
tags:
  - phase-13
  - public-api
  - self-management
  - sdk-sync
---

<objective>
Ship the API-key self-management surface (both the /api/v1 SDK-side endpoints and the /user web-side endpoints that the Settings UI in Plan 13-06 will call) and the sync `Client` half of bindwave-python with working `client.jobs.submit/get/list/cancel` + `client.api_keys.list/revoke` + the exception hierarchy + Idempotency-Key auto-generation. SDK is respx-tested against the /api/v1 surface that Plan 13-03 just shipped; backend self-management endpoints are tested via the same dependency_overrides + ASGITransport pattern as /api/v1/jobs. Backend web endpoints (`/user/api-keys`) mirror the existing auth/signup mint-once flow (plaintext shown exactly once in POST response; GET never returns plaintext). Per RESEARCH §5.5, frontend/src/lib/api.ts opt-out list is NOT modified — web flow uses /user/api-keys with X-Org-Id; SDK uses /api/v1/api-keys without X-Org-Id.

Purpose: Complete the API surface so Plan 13-05 can flesh out the AsyncClient + auto-paginator + Job.wait_until_complete on a working sync base, and Plan 13-06 can build the Settings tab against the /user/api-keys web endpoints.

Output: 2 new backend endpoint files + 1 web router modification + 9 SDK files (real implementations replacing Plan 13-02 placeholders) + 3 SDK test files + 1 SDK example + README update + filled-in backend test stub + updated snapshot fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-public-api/13-RESEARCH.md
@.planning/phases/13-public-api/13-PATTERNS.md
@.planning/phases/13-public-api/13-02-PLAN.md
@.planning/phases/13-public-api/13-03-PLAN.md

<interfaces>
Key analog excerpts (from PATTERNS.md).

From backend/api/v1/jobs.py (just-created in 13-03 — the ownership + role-guard pattern to mirror):
- router = APIRouter(prefix="/jobs", tags=["api_v1_jobs"])
- async def submit_job(..., org_id: str = Depends(require_role_api("owner", "admin", "member")))
- WHERE organization_id = $1 — every SELECT gated by org

From backend/auth/router.py:81-150 signup (analog for /user/api-keys POST mint-once flow):
- @router.post("/signup")
- @limiter.limit("3/minute;10/hour")
- async def signup(request: Request, body: SignUpRequest, response: Response):
- supabase = _get_supabase(); result = supabase.auth.sign_up({"email": ..., "password": ...})

From backend/user/router.py:96-110 (analog for /user/api-keys CRUD with require_role):
- @router.get("/settings")
- async def get_settings(user_id: str = Depends(get_current_user), org_id: str = Depends(get_active_org_id_only)):
- pool = await get_db_pool()
- async with pool.acquire() as conn: row = await conn.fetchrow("SELECT ... WHERE organization_id = $1", org_id)

From PATTERNS.md "backend/user/api_keys.py" section (the full router shape):
- router = APIRouter(prefix="/user/api-keys", tags=["user_api_keys"])
- @router.get("/") list_my_keys (require_role members allowed)
- @router.post("/") create_key — generate_api_key, INSERT, return {plaintext, prefix, id, name, created_at}
- @router.post("/{key_id}/revoke") revoke_my_key (owners + admins only)

From RESEARCH §2.5 lines 175-185 (SDK client architecture):
- Default base_url = "https://api.bindwave.com/api/v1"
- Auth header: Authorization: Bearer {api_key}, NO X-Org-Id (D-01)
- Reads BINDWAVE_API_KEY env if no api_key= kwarg
- 5xx + 429 retry with exponential backoff capped at 5 attempts
- Idempotency-Key: SDK auto-generates uuid.uuid4().hex if caller doesn't supply
- Exception parsing reads application/problem+json and routes by type URL slug

From PATTERNS.md "bindwave-python/src/bindwave/_client.py + _async_client.py + jobs.py + api_keys.py" section:
- Sync Client class with .jobs (JobsResource) and .api_keys (ApiKeysResource) attributes
- httpx.Client transport
- BindwaveError hierarchy: BindwaveAuthError (401), BindwaveRateLimitError (429), BindwaveValidationError (400+422), BindwaveJobError (other 4xx), BindwaveAPIError (5xx)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend self-management endpoints (/api/v1/api-keys + /user/api-keys) + filled-in backend test</name>
  <files>
    backend/api/v1/api_keys.py
    backend/api/v1/__init__.py
    backend/user/api_keys.py
    backend/user/router.py
    backend/tests/api_v1/test_api_keys.py
    backend/tests/contract/_openapi_paths_snapshot.txt
  </files>
  <behavior>
    - GET /api/v1/api-keys returns {data: [{id, name, prefix, created_at, last_used_at}, ...]} for caller's org (revoked excluded)
    - POST /api/v1/api-keys/{key_id}/revoke (SDK self-revoke) UPDATEs revoked_at; returns {id, revoked_at} on success
    - POST /api/v1/api-keys/{key_id}/revoke returns 404 (problem+json) when key belongs to different org
    - GET /user/api-keys returns list of caller's org keys (web auth via require_role; X-Org-Id header expected)
    - POST /user/api-keys creates a key, returns {id, name, prefix, plaintext, role, created_at} — plaintext is shown EXACTLY ONCE
    - GET /user/api-keys NEVER returns plaintext field (only prefix)
    - POST /user/api-keys/{key_id}/revoke flips revoked_at; subsequent GET excludes the row
    - POST /user/api-keys validates body name is non-blank (CHECK constraint at DB; Pydantic also rejects)
    - Owner/admin can revoke any key in org; member can revoke only their own (via created_by_user_id check) — applies to BOTH /api/v1 and /user paths
    - /user/api-keys does NOT appear in /api/openapi.json — the new APIRouter is constructed with include_in_schema=False DIRECTLY (FastAPI does NOT inherit include_in_schema from a parent router; the parent /user router carrying include_in_schema=False from 13-01 is NOT sufficient to hide a child router)
  </behavior>
  <read_first>
    - backend/api/v1/jobs.py (just-created in 13-03 — confirm router pattern + require_role_api usage)
    - backend/api/v1/__init__.py (just-created in 13-03 — must append api_keys_router.include_router call)
    - backend/auth/router.py:81-150 (signup mint-once pattern)
    - backend/user/router.py (full file — confirm existing /user routes + APIRouter constructor with the include_in_schema=False from 13-01; new /user/api-keys subrouter MUST explicitly carry its own include_in_schema=False — the parent flag does NOT propagate)
    - backend/auth/api_keys.py (just-created in 13-02 — generate_api_key signature)
    - backend/auth/org_dependencies.py (require_role signature — distinguish from require_role_api)
    - .planning/phases/13-public-api/13-PATTERNS.md "backend/api/v1/api_keys.py" + "backend/user/api_keys.py" sections (the full router bodies)
    - backend/tests/contract/_openapi_paths_snapshot.txt (current state from 13-03 — 3 paths)
  </read_first>
  <action>
    Create `backend/api/v1/api_keys.py` per PATTERNS.md "backend/api/v1/api_keys.py" section. Imports: from fastapi import APIRouter, Depends, HTTPException, Request, status; from auth.api_key_dependencies import require_role_api; from middleware.rate_limit import api_v1_limiter; from db.connection import get_db_pool; from config import settings.

    `router = APIRouter(prefix="/api-keys", tags=["api_v1_api_keys"])`.

    GET endpoint: `@router.get("/")` then `@api_v1_limiter.limit(settings.api_v1_rate_limit)` then `async def list_keys(request: Request, org_id: str = Depends(require_role_api("owner", "admin", "member")))`. Body: acquire pool, fetch `SELECT id, name, prefix, created_at, last_used_at FROM public.api_keys WHERE organization_id = $1 AND revoked_at IS NULL ORDER BY created_at DESC`. Return `{"data": [{"id": str(r["id"]), "name": r["name"], "prefix": r["prefix"], "created_at": r["created_at"].isoformat(), "last_used_at": r["last_used_at"].isoformat() if r["last_used_at"] else None} for r in rows]}`.

    Revoke endpoint: `@router.post("/{key_id}/revoke")` then `@api_v1_limiter.limit(settings.api_v1_rate_limit)` then `async def revoke_key(request: Request, key_id: str, org_id: str = Depends(require_role_api("owner", "admin")))`. Body: acquire pool, execute `UPDATE public.api_keys SET revoked_at = now() WHERE id = $1 AND organization_id = $2 AND revoked_at IS NULL`. If result == "UPDATE 0" → raise HTTPException(404, "API key not found"). Return `{"id": key_id, "revoked_at": <now iso>}`.

    Modify `backend/api/v1/__init__.py`. Append: `from api.v1.api_keys import router as api_keys_router; router.include_router(api_keys_router)`. After this, the aggregator includes both jobs_router AND api_keys_router under /api/v1.

    Create `backend/user/api_keys.py` per PATTERNS.md "backend/user/api_keys.py" section. Imports: from fastapi import APIRouter, Depends, HTTPException, Request, status; from pydantic import BaseModel, Field; from auth.dependencies import get_current_user; from auth.org_dependencies import require_role, get_active_org; from auth.api_keys import generate_api_key; from middleware.rate_limit import limiter; from db.connection import get_db_pool.

    `router = APIRouter(prefix="/user/api-keys", tags=["user_api_keys"], include_in_schema=False)`. This `include_in_schema=False` MUST be set directly on this child router constructor — FastAPI does NOT inherit the flag from the parent /user router constructed in 13-01. The D-15 verify in this task asserts /user/api-keys is absent from /api/openapi.json regardless of whether the parent router also carries the flag.

    Pydantic model `class CreateKeyRequest(BaseModel): name: str = Field(min_length=1, max_length=80, strip_whitespace=True)`.

    GET endpoint per PATTERNS.md: list_my_keys depends on `Depends(require_role("owner", "admin", "member"))` returning org_id, fetch + return same shape as /api/v1/api-keys but as a plain array (matches frontend interface ApiKey[]).

    POST endpoint: create_key takes body + depends on both user_id (from get_current_user, to record created_by_user_id) and (org_id, role) from get_active_org (so we have role_at_creation). Generate via `plaintext, prefix, h = generate_api_key(env="live")`. INSERT row: `INSERT INTO public.api_keys (organization_id, created_by_user_id, name, prefix, bcrypt_hash, role_at_creation) VALUES ($1, $2, $3, $4, $5, $6::public.org_role) RETURNING id, created_at`. Return `{"id": str(row["id"]), "name": body.name, "prefix": prefix, "plaintext": plaintext, "role": role, "created_at": row["created_at"].isoformat()}`. Apply `@limiter.limit("10/hour")` to throttle bot-spray (mirrors auth/router.py:82 pattern).

    Revoke endpoint: depends on `Depends(require_role("owner", "admin"))` returning org_id. UPDATE WHERE id = $1 AND organization_id = $2 AND revoked_at IS NULL. 404 on UPDATE 0.

    Modify `backend/user/router.py` (the /user package router file, NOT the user subrouter). After the existing `from user.router import router as user_router` style structure, ensure `from user.api_keys import router as api_keys_router` is imported and `user_router.include_router(api_keys_router)` is called — OR if /user/api-keys is registered standalone in main.py, add that to backend/main.py. Pick the path that matches existing /user structure (likely: include_router inside backend/user/router.py because that's how /user/settings is structured). If the structure is to register the api_keys router separately in main.py, add the include there instead — but DO NOT add to the /api/v1 aggregator.

    Update `backend/tests/contract/_openapi_paths_snapshot.txt`. Add `/api/v1/api-keys` and `/api/v1/api-keys/{key_id}/revoke` to the snapshot (alphabetically sorted). The /user/api-keys paths are absent because the child router carries include_in_schema=False directly (the D-15 verify below asserts this).

    Fill in `backend/tests/api_v1/test_api_keys.py`. Already has test_generate_api_key_format / test_verify_api_key_constant_time / test_pepper_rotation from 13-02. Add now: test_create_returns_plaintext (POST /user/api-keys returns plaintext; GET on same row excludes it), test_revoked_key_rejects (synthetic key in DB with revoked_at=now; get_current_api_key dep raises 401), test_api_v1_list_org_scoped (insert keys for 2 orgs; GET /api/v1/api-keys returns only caller's org), test_revoke_404_cross_org (caller in org-A tries to revoke key in org-B → 404 problem+json).
  </action>
  <verify>
    <automated>
      test -f backend/api/v1/api_keys.py
      test -f backend/user/api_keys.py
      grep -q "router.include_router(api_keys_router)" backend/api/v1/__init__.py
      # F8 — D-15 verify: assert /user/api-keys explicitly hidden from OpenAPI spec. FastAPI does NOT inherit include_in_schema from a parent router, so the child router MUST carry the flag itself (verified at source by the next grep and at runtime by the TestClient assertion below).
      grep -q 'APIRouter(prefix="/user/api-keys".*include_in_schema=False' backend/user/api_keys.py
      cd backend && python -c "from fastapi.testclient import TestClient; from main import app; client = TestClient(app); r = client.get('/api/openapi.json'); assert not any('/user/api-keys' in p for p in r.json()['paths']), 'D-15 violation: /user/api-keys leaked into spec'"
      cd backend && pytest tests/api_v1/test_api_keys.py -x
      cd backend && pytest tests/contract/test_openapi_snapshot.py -x  # snapshot now matches 5-path fixture
      cd backend && python -c "from fastapi.testclient import TestClient; from main import app; client = TestClient(app); r = client.get('/api/openapi.json'); paths = sorted(r.json()['paths'].keys()); assert paths == ['/api/v1/api-keys', '/api/v1/api-keys/{key_id}/revoke', '/api/v1/jobs', '/api/v1/jobs/{job_id}', '/api/v1/jobs/{job_id}/cancel'], paths"
      cd backend && pytest tests -x  # FULL backend suite; everything green
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/api/v1/api_keys.py defines router with 2 endpoints
    - backend/api/v1/__init__.py includes api_keys_router
    - backend/user/api_keys.py defines router with 3 endpoints (list, create, revoke) and `APIRouter(prefix="/user/api-keys", tags=["user_api_keys"], include_in_schema=False)` literally on the constructor line — NOT relying on parent-router inheritance, because FastAPI does not propagate the flag
    - POST /user/api-keys uses Pydantic Field(min_length=1) so blank name returns 422 BEFORE hitting DB CHECK constraint
    - POST /user/api-keys response body contains both `plaintext` and `prefix` keys
    - GET /user/api-keys response body does NOT contain `plaintext` field
    - Snapshot fixture file lists 5 paths (3 jobs + 2 api-keys), alphabetically sorted; /user/api-keys absent
    - F8 — D-15 runtime verify: `TestClient(app).get('/api/openapi.json')` returns a spec with NO path string containing `/user/api-keys` (asserted by the new TestClient one-liner in this task's verify block)
    - All 7+ tests in test_api_keys.py pass
    - Full backend test suite passes: `pytest backend/tests -x` exits 0
  </acceptance_criteria>
  <done>Backend has 5 API endpoints under /api/v1 (3 jobs + 2 api-keys) and 3 web endpoints under /user/api-keys (list, create, revoke). Plaintext mint-once pattern enforced. Org-scoped + role-gated. Snapshot test locks the 5-path published surface. D-15 hidden-router invariant runtime-asserted: /user/api-keys does not leak into the spec because the child router carries include_in_schema=False directly.</done>
</task>

<task type="auto">
  <name>Task 2: bindwave-python sync Client + JobsResource + ApiKeysResource + exception hierarchy + tests</name>
  <files>
    bindwave-python/src/bindwave/_client.py
    bindwave-python/src/bindwave/jobs.py
    bindwave-python/src/bindwave/api_keys.py
    bindwave-python/src/bindwave/_exceptions.py
    bindwave-python/src/bindwave/_idempotency.py
    bindwave-python/src/bindwave/types/__init__.py
    bindwave-python/src/bindwave/types/job.py
    bindwave-python/src/bindwave/types/api_key.py
    bindwave-python/src/bindwave/__init__.py
    bindwave-python/tests/test_client.py
    bindwave-python/tests/test_jobs.py
    bindwave-python/tests/test_api_keys.py
    bindwave-python/examples/submit_and_wait.py
    bindwave-python/README.md
  </files>
  <read_first>
    - bindwave-python/src/bindwave/__init__.py (from Plan 13-02 — confirm public re-exports currently point at placeholders; this task replaces those with real classes)
    - .planning/phases/13-public-api/13-RESEARCH.md §2.5 lines 175-185 (SDK behavior: auth header, retry, Idempotency-Key auto-gen, exception parsing)
    - .planning/phases/13-public-api/13-PATTERNS.md "bindwave-python/src/bindwave/_client.py + ... + api_keys.py" section (full pattern reference to Anthropic SDK)
    - backend/api/v1/jobs.py (just-created in 13-03 — confirm request/response envelopes the SDK must match)
    - backend/api/v1/api_keys.py (just-created in this plan — confirm response envelopes)
    - .planning/phases/13-public-api/13-RESEARCH.md §2.9 (Idempotency-Key behavior — SDK should auto-supply via uuid4)
    - .planning/phases/13-public-api/13-RESEARCH.md §2.7 (RFC 7807 envelope — SDK exception parser reads `type` URL slug)
  </read_first>
  <action>
    Create `bindwave-python/src/bindwave/_exceptions.py`. Exception hierarchy per D-16 / RESEARCH §2.5: `class BindwaveError(Exception)` base with attrs status_code: int, type_url: str | None, problem_body: dict | None. Subclasses inherit and set defaults: `BindwaveAuthError` (status 401), `BindwaveRateLimitError` (status 429, attr retry_after: int | None), `BindwaveValidationError` (status 400 or 422, attr errors: list), `BindwaveJobError` (other 4xx like 402 Payment Required, 409 Conflict), `BindwaveAPIError` (5xx). Helper `def parse_error_response(response: httpx.Response) -> BindwaveError`: if response.headers.get("content-type", "").startswith("application/problem+json"), load body and route by status code: 401 → BindwaveAuthError, 429 → BindwaveRateLimitError(retry_after=int(response.headers.get("Retry-After", 0)) or None), 400/422 → BindwaveValidationError(errors=body.get("errors", [])), 4xx → BindwaveJobError, 5xx → BindwaveAPIError. Each raised instance gets `problem_body=body`, `type_url=body.get("type")`, message = body.get("detail", body.get("title", str(response.status_code))).

    Create `bindwave-python/src/bindwave/_idempotency.py`. Trivial: `import uuid; def generate_idempotency_key() -> str: return uuid.uuid4().hex`.

    Create `bindwave-python/src/bindwave/types/__init__.py` re-exporting from job.py and api_key.py.

    Create `bindwave-python/src/bindwave/types/job.py`. Pydantic models: `class Candidate(BaseModel): rank: int; pdb_key: str; scores: dict; download_url: str`. `class JobStatus(str, Enum): QUEUED = "queued"; RUNNING = "running"; COMPLETE = "complete"; FAILED = "failed"; CANCELLED = "cancelled"`. `class Job(BaseModel): id: str; tool: str; status: JobStatus; name: str | None = None; created_at: datetime; completed_at: datetime | None = None; organization_id: str | None = None; gpu_cost_usd: float | None = None; candidates: list[Candidate] = []`. Use ConfigDict(extra="ignore") so extra fields from backend response don't break parsing.

    Create `bindwave-python/src/bindwave/types/api_key.py`. `class ApiKey(BaseModel): id: str; name: str; prefix: str; created_at: datetime; last_used_at: datetime | None = None`.

    Create `bindwave-python/src/bindwave/_client.py`. Sync Client class. Imports: import os; from typing import Any; import httpx; from bindwave._exceptions import parse_error_response; from bindwave._idempotency import generate_idempotency_key; from bindwave.jobs import JobsResource; from bindwave.api_keys import ApiKeysResource. `class Client`:
    (a) `__init__(self, api_key: str | None = None, base_url: str = "https://api.bindwave.com/api/v1", timeout: float = 60.0, max_retries: int = 5)`:
        - api_key = api_key or os.environ.get("BINDWAVE_API_KEY"); if not api_key: raise ValueError("api_key is required (or set BINDWAVE_API_KEY env var)")
        - self._api_key = api_key; self._base_url = base_url.rstrip("/"); self._max_retries = max_retries
        - self._http = httpx.Client(base_url=base_url, timeout=timeout, headers={"Authorization": f"Bearer {api_key}", "User-Agent": f"bindwave-python/0.1.0"})
        - self.jobs = JobsResource(self)
        - self.api_keys = ApiKeysResource(self)
    (b) `def _request(self, method: str, path: str, *, json_body: dict | None = None, params: dict | None = None, idempotency_key: str | None = None, retry_attempt: int = 0) -> httpx.Response`:
        - headers = {}
        - if method == "POST" and json_body is not None and "/jobs" in path and "/cancel" not in path:
            - headers["Idempotency-Key"] = idempotency_key or generate_idempotency_key()
        - try: response = self._http.request(method, path, json=json_body, params=params, headers=headers)
        - except httpx.NetworkError: if retry_attempt < self._max_retries: time.sleep(min(2 ** retry_attempt, 30)); return self._request(method, path, json_body=json_body, params=params, idempotency_key=headers.get("Idempotency-Key"), retry_attempt=retry_attempt + 1)
        - if response.status_code == 429 and retry_attempt < self._max_retries:
            - wait = int(response.headers.get("Retry-After", "1"))
            - time.sleep(min(wait, 30))
            - return self._request(method, path, json_body=json_body, params=params, idempotency_key=headers.get("Idempotency-Key"), retry_attempt=retry_attempt + 1)
        - if 500 <= response.status_code < 600 and retry_attempt < self._max_retries:
            - time.sleep(min(2 ** retry_attempt, 30))
            - return self._request(method, path, json_body=json_body, params=params, idempotency_key=headers.get("Idempotency-Key"), retry_attempt=retry_attempt + 1)
        - if response.status_code >= 400: raise parse_error_response(response)
        - return response
    (c) `def close(self) -> None: self._http.close()`
    (d) Context manager support: `__enter__` / `__exit__`.

    Create `bindwave-python/src/bindwave/jobs.py`. `class JobsResource`:
    (a) `__init__(self, client): self._client = client`
    (b) `def submit(self, *, tool: str, parameters: dict, name: str | None = None, idempotency_key: str | None = None) -> Job`: body = {"tool": tool, "parameters": parameters}; if name: body["name"] = name; response = self._client._request("POST", "/jobs", json_body=body, idempotency_key=idempotency_key); return Job.model_validate(response.json())
    (c) `def get(self, job_id: str) -> Job`: response = self._client._request("GET", f"/jobs/{job_id}"); return Job.model_validate(response.json())
    (d) `def list(self, *, limit: int = 25, cursor: str | None = None, status: str | None = None, tool: str | None = None, created_after: str | None = None, created_before: str | None = None) -> JobListPage`: params = {k:v for k,v in {"limit": limit, "cursor": cursor, "status": status, "tool": tool, "created_after": created_after, "created_before": created_before}.items() if v is not None}; response = self._client._request("GET", "/jobs", params=params); data = response.json(); return JobListPage(data=[Job.model_validate(j) for j in data["data"]], next_cursor=data.get("next_cursor"))
    (e) `def cancel(self, job_id: str) -> Job`: response = self._client._request("POST", f"/jobs/{job_id}/cancel"); return Job.model_validate(response.json())

    Create `bindwave-python/src/bindwave/api_keys.py`. `class ApiKeysResource`: `list() -> list[ApiKey]` (calls GET /api-keys, parses response.json()["data"]), `revoke(key_id: str) -> dict` (POST /api-keys/{key_id}/revoke).

    Update `bindwave-python/src/bindwave/__init__.py`. Replace the placeholder classes from Plan 13-02 with real imports: `from bindwave._client import Client; from bindwave.jobs import JobsResource, JobListPage; from bindwave.api_keys import ApiKeysResource; from bindwave._exceptions import (...); from bindwave.types.job import Job, JobStatus, Candidate; from bindwave.types.api_key import ApiKey`. NOTE: AsyncClient is still placeholder (Plan 13-05 ships it). Re-export AsyncClient as a placeholder NotImplementedError stub for now.

    Add `JobListPage` Pydantic model (in jobs.py or types/job.py): `class JobListPage(BaseModel): data: list[Job]; next_cursor: str | None = None`.

    Create `bindwave-python/tests/test_client.py`. Tests:
    - test_constructor_requires_api_key (no env, no kwarg → ValueError)
    - test_constructor_reads_env_var (monkeypatch BINDWAVE_API_KEY → no error)
    - test_authorization_header_set (instantiate, peek at self._http.headers, assert "Bearer bw_test_x")
    - test_no_x_org_id_header (assert "X-Org-Id" NOT in headers — D-01)

    Create `bindwave-python/tests/test_jobs.py`. Uses respx to mock backend:
    - test_submit_returns_job (mock POST /jobs returns {"id": "j-1", "status": "queued", "tool": "rfdiffusion", "created_at": "..."}; client.jobs.submit(...) returns Job(id="j-1", status=JobStatus.QUEUED))
    - test_submit_auto_idempotency_key (mock asserts Idempotency-Key header present + matches uuid4 hex pattern when caller doesn't supply)
    - test_submit_caller_idempotency_key (caller supplies idempotency_key="my-key"; mock asserts header == "my-key")
    - test_get (mock GET /jobs/j-1 → returns Job)
    - test_list (mock GET /jobs?limit=25 → returns JobListPage)
    - test_cancel (mock POST /jobs/j-1/cancel → returns Job)
    - test_429_retries (mock returns 429 with Retry-After: 0, then 200 — client retries successfully)
    - test_401_raises_BindwaveAuthError (mock returns 401 problem+json → exception type matches)
    - test_422_raises_BindwaveValidationError (mock returns 422 problem+json with errors[] → exception .errors matches)
    - test_5xx_retries_then_BindwaveAPIError (mock returns 500 five times → after max retries, raise BindwaveAPIError)

    Create `bindwave-python/tests/test_api_keys.py`. Mock-based tests for client.api_keys.list() + client.api_keys.revoke().

    Create `bindwave-python/examples/submit_and_wait.py`. 15-line example per D-09 / RESEARCH §2.5: import Client; client = Client(); job = client.jobs.submit(tool="rfdiffusion", parameters={...}); job = client.jobs.get(job.id); while job.status != JobStatus.COMPLETE: time.sleep(30); job = client.jobs.get(job.id); print(f"Got {len(job.candidates)} candidates").

    Update `bindwave-python/README.md`. Replace 13-02's contract-only placeholder with the real quickstart matching examples/submit_and_wait.py.
  </action>
  <verify>
    <automated>
      test -f bindwave-python/src/bindwave/_client.py
      test -f bindwave-python/src/bindwave/jobs.py
      test -f bindwave-python/src/bindwave/api_keys.py
      test -f bindwave-python/src/bindwave/_exceptions.py
      cd bindwave-python && python -c "import sys; sys.path.insert(0, 'src'); from bindwave import Client, JobListPage; c = Client(api_key='bw_test_xxx'); assert c._http.headers['Authorization'] == 'Bearer bw_test_xxx'; assert 'X-Org-Id' not in c._http.headers"
      cd bindwave-python && python -c "import sys; sys.path.insert(0, 'src'); from bindwave._exceptions import BindwaveError, BindwaveAuthError, BindwaveRateLimitError, BindwaveValidationError, BindwaveJobError, BindwaveAPIError; assert issubclass(BindwaveAuthError, BindwaveError)"
      cd bindwave-python && python -c "import sys; sys.path.insert(0, 'src'); from bindwave._idempotency import generate_idempotency_key; k = generate_idempotency_key(); assert len(k) == 32"
      cd bindwave-python && PYTHONPATH=src python -m pytest tests/test_client.py tests/test_jobs.py tests/test_api_keys.py -x
    </automated>
  </verify>
  <acceptance_criteria>
    - bindwave-python `from bindwave import Client` returns real class (not placeholder); `Client(api_key="bw_test_x")` does not raise NotImplementedError
    - Client._http.headers["Authorization"] == "Bearer bw_test_x"; "X-Org-Id" NOT in headers (D-01 verified)
    - client.jobs.submit / get / list / cancel implemented (callable, accept correct kwargs)
    - client.api_keys.list / revoke implemented
    - BindwaveError hierarchy: BindwaveAuthError, BindwaveRateLimitError, BindwaveValidationError, BindwaveJobError, BindwaveAPIError all inherit BindwaveError
    - parse_error_response routes 401 → BindwaveAuthError, 429 → BindwaveRateLimitError (sets retry_after), 422 → BindwaveValidationError (sets errors)
    - generate_idempotency_key returns 32-char hex
    - All 13+ test cases in test_client.py + test_jobs.py + test_api_keys.py pass with respx mocking
    - 429 retry path: mock returns 429 once then 200; client.jobs.get succeeds
    - 5xx retry path: mock returns 500 five times → after max_retries, raises BindwaveAPIError
    - examples/submit_and_wait.py file exists, parses as valid Python, follows D-09 (15 lines max)
    - bindwave-python/README.md contains real example with `pip install bindwave` install instruction
  </acceptance_criteria>
  <done>SDK sync half is functional. Client + JobsResource + ApiKeysResource + exception parsing + Idempotency-Key auto-gen + 429/5xx retry all implemented and respx-tested. AsyncClient remains placeholder (13-05 ships it). Plan 13-05 + 13-06 can build on this.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /user/api-keys POST response | Plaintext key crosses the network ONCE — caller is responsible for saving. After response sent, only prefix is queryable. |
| /api/v1/api-keys revoke | Self-revoke by SDK call uses the same key being revoked; the dep call validates the key is non-revoked BEFORE running the UPDATE, so there's no chicken-and-egg |
| SDK Authorization header | Bearer token in every request; httpx auto-includes via Client constructor headers; never logged by the SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information disclosure | Plaintext on GET | mitigate | /user/api-keys GET response builder explicitly excludes the plaintext field; only the prefix is selectable. Snapshot test (Plan 13-07) asserts plaintext is not in any GET response schema. |
| T-13-02 | Tampering | Revoke replay | mitigate | UPDATE ... WHERE revoked_at IS NULL means second revoke is a no-op (UPDATE 0 → 404). Subsequent dep calls reject 401. |
| T-13-03 | Elevation of privilege | Member revoking owner key | mitigate | /api/v1/api-keys/{key_id}/revoke and /user/api-keys/{key_id}/revoke both require_role("owner", "admin"); member role attempts → 403 problem+json (v1) or 403 JSON (web flow). |
| T-13-09 | Information disclosure | Cross-org key listing | mitigate | Every SELECT gated `WHERE organization_id = $1`; partial index `idx_api_keys_org` covers the hot path |
| T-13-04 | Tampering | SDK Idempotency-Key replay | mitigate | SDK auto-generates fresh uuid4 per submit() call; caller MAY override; backend 3-state lifecycle (Plan 13-03) handles replay. Tested in test_jobs.py. |
</threat_model>

<verification>
- `pytest backend/tests -x` passes
- `cd bindwave-python && PYTHONPATH=src python -m pytest tests/ -x` passes
- TestClient GET /api/openapi.json returns exactly 5 paths matching the updated snapshot fixture
- TestClient GET /api/openapi.json returns NO path string containing `/user/api-keys` (D-15 hidden-router invariant)
- `from bindwave import Client; Client('bw_test_x').jobs.submit(tool='rfdiffusion', parameters={})` no longer raises NotImplementedError (respx-mocked)
</verification>

<success_criteria>
1. /api/v1/api-keys GET returns {data: [...]} list of caller's org keys
2. /api/v1/api-keys/{id}/revoke 404s on cross-org
3. /user/api-keys POST returns plaintext exactly once; GET excludes it
4. /user/api-keys APIRouter is constructed with include_in_schema=False directly (not relying on parent inheritance); TestClient confirms /api/openapi.json contains no /user/api-keys path
5. bindwave.Client functional with real implementations replacing 13-02 placeholders
6. client.jobs.submit/get/list/cancel + client.api_keys.list/revoke all working against /api/v1
7. Idempotency-Key auto-generated via uuid4 when caller doesn't supply
8. 429 retry honors Retry-After; 5xx retries with exponential backoff
9. RFC 7807 problem+json routes to typed exceptions (BindwaveAuthError, BindwaveValidationError, etc.)
10. Authorization: Bearer header sent; X-Org-Id NEVER sent (D-01)
11. _openapi_paths_snapshot.txt now contains 5 paths, all under /api/v1/
12. AsyncClient remains placeholder (Plan 13-05)
</success_criteria>

<output>
Create `.planning/phases/13-public-api/13-04-SUMMARY.md` when done.
</output>
</content>
</invoke>