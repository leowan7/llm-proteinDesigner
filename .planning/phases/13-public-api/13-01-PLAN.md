---
phase: 13-public-api
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - backend/config.py
  - backend/main.py
  - backend/auth/router.py
  - backend/agent/router.py
  - backend/admin/router.py
  - backend/billing/router.py
  - backend/debug_routes.py
  - backend/jobs/router.py
  - backend/organizations/router.py
  - backend/pdb_utils/router.py
  - backend/sessions/router.py
  - backend/user/router.py
  - backend/webhooks/router.py
  - supabase/migrations/20260607000001_api_keys.sql
  - supabase/migrations/20260607000002_api_key_idempotency.sql
  - backend/tests/api_v1/__init__.py
  - backend/tests/api_v1/conftest.py
  - backend/tests/api_v1/test_api_keys.py
  - backend/tests/api_v1/test_auth.py
  - backend/tests/api_v1/test_idempotency.py
  - backend/tests/api_v1/test_pagination.py
  - backend/tests/api_v1/test_cursor.py
  - backend/tests/api_v1/test_jobs_get.py
  - backend/tests/api_v1/test_errors.py
  - backend/tests/api_v1/test_rate_limit.py
  - backend/tests/contract/__init__.py
  - backend/tests/contract/test_openapi_snapshot.py
  - backend/tests/contract/test_routers_hidden.py
  - backend/tests/contract/_openapi_paths_snapshot.txt
autonomous: false  # Contains [BLOCKING] supabase db push checkpoint
requirements:
  - API-09
  - API-11
  - PLAT-V2-01
user_setup:
  - service: supabase
    why: "Apply two new migrations (api_keys + api_key_idempotency tables) to live DB"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Already configured in Phase 11 deploy infra; reuse same token"
    dashboard_config: []
must_haves:
  truths:
    - "REQUIREMENTS.md lists API-01..API-12 with full descriptions"
    - "REQUIREMENTS.md marks PLAT-V2-01 as 'Validated (Phase 13)'"
    - "ROADMAP.md Phase 13 SC 6 reads 'pip install bindwave' (not 'kendrew')"
    - "api_keys table exists in live DB with 11 columns + 2 partial indexes per RESEARCH §2.2"
    - "api_key_idempotency table exists with composite PK + status column per RESEARCH §2.9"
    - "Pydantic Settings exposes api_key_pepper, api_key_pepper_prev, api_v1_rate_limit, idempotency_ttl_hours"
    - "main.py FastAPI constructor uses title='Bindwave Public API', docs_url='/api/docs'"
    - "All 12 legacy routers + /health route carry include_in_schema=False"
    - "backend/tests/api_v1/ exists with all 9 scaffold files (init + conftest + 8 test stubs)"
    - "backend/tests/contract/ exists with snapshot fixture file + 2 test scaffolds"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      contains: "API-01"
      contains_also: "PLAT-V2-01 | Phase 13 | Validated"
    - path: ".planning/ROADMAP.md"
      contains: "pip install bindwave"
      not_contains: "pip install kendrew"
    - path: "backend/config.py"
      provides: "api_key_pepper, api_key_pepper_prev, api_v1_rate_limit, idempotency_ttl_hours"
    - path: "backend/main.py"
      provides: "Bindwave Public API title + /api/docs route + include_in_schema=False on /health"
    - path: "supabase/migrations/20260607000001_api_keys.sql"
      provides: "api_keys table per RESEARCH §2.2"
    - path: "supabase/migrations/20260607000002_api_key_idempotency.sql"
      provides: "api_key_idempotency 3-state table per RESEARCH §2.9"
    - path: "backend/tests/api_v1/conftest.py"
      provides: "synthetic_api_key + idempotency_key + override_api_key fixtures"
    - path: "backend/tests/contract/_openapi_paths_snapshot.txt"
      provides: "Locked snapshot of post-flip OpenAPI paths"
  key_links:
    - from: "backend/main.py"
      to: "FastAPI app metadata"
      via: "openapi_url + docs_url constructor kwargs"
      pattern: 'docs_url=.?/api/docs.?'
    - from: "All 12 legacy routers"
      to: "OpenAPI schema visibility"
      via: "APIRouter(... include_in_schema=False)"
      pattern: "include_in_schema=False"
    - from: "backend/tests/contract/_openapi_paths_snapshot.txt"
      to: "Published API surface contract"
      via: "Snapshot of sorted(spec['paths'].keys())"
      pattern: "^/api/v1/"
tags:
  - phase-13
  - public-api
  - schema
  - openapi-surface
  - foundation
---

<objective>
Land the Wave 0 foundation for Phase 13: REQUIREMENTS.md mints API-01..API-12 (RESEARCH §3) and promotes PLAT-V2-01 to Phase 13/Validated; ROADMAP.md SC 6 corrected from `kendrew` to `bindwave` (D-09 + CONTEXT.md ref); Pydantic Settings adds 4 new fields (api_key_pepper, api_key_pepper_prev, api_v1_rate_limit, idempotency_ttl_hours); two new Supabase migrations create api_keys + api_key_idempotency tables; all 12 legacy routers + `/health` route get `include_in_schema=False` so only `/api/v1/*` paths surface in the OpenAPI spec; FastAPI app metadata flipped to `title="Bindwave Public API"` + `docs_url="/api/docs"`; full Wave 0 test scaffolds (backend/tests/api_v1/ + backend/tests/contract/) created with conftest fixtures from RESEARCH §4.4; OpenAPI snapshot fixture file seeded with the planned post-D-15 surface. Includes the [BLOCKING] `supabase db push` checkpoint after migration commit.

Purpose: This plan is the precondition for every other plan in Phase 13. Without it, downstream plans have nowhere to write tests, no schema to insert into, no settings to read pepper from, and no contract to lock the published surface against.

Output: REQUIREMENTS.md + ROADMAP.md edits, 2 migration files, config.py + main.py edits, 12 router edits, 11 new test scaffold files, 1 snapshot fixture text file. Supabase schema pushed to live DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-public-api/13-RESEARCH.md
@.planning/phases/13-public-api/13-PATTERNS.md
@.planning/phases/13-public-api/13-VALIDATION.md

<interfaces>
Key analog file:line excerpts the executor needs (extracted from PATTERNS.md). Use these directly; no codebase exploration needed.

From supabase/migrations/20260420000001_job_tier_and_budget.sql (CREATE TABLE + partial index + COMMENT pattern):
- ALTER TABLE ... ADD COLUMN IF NOT EXISTS ... NOT NULL DEFAULT 'pilot' CHECK (... IN (...))
- CREATE INDEX IF NOT EXISTS idx_jobs_user_tier_tool_status ON public.jobs (user_id, job_tier, status) WHERE status = 'complete'
- COMMENT ON COLUMN public.jobs.job_tier IS '...'

From supabase/migrations/20260318000000_init.sql:10-19 (PK + foreign-key + timestamp pattern):
- id UUID PRIMARY KEY DEFAULT gen_random_uuid()
- user_id UUID NOT NULL REFERENCES public.users(id) ON DELETE CASCADE
- created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
- updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()

From supabase/migrations/20260605000001_organizations.sql (per-org partial index pattern):
- public.org_role ENUM already exists at :18 (values: owner, admin, member)
- ON DELETE CASCADE to organizations at :27-36
- CONSTRAINT name_not_blank CHECK (length(btrim(name)) > 0) at :35
- partial index pattern: WHERE accepted_at IS NULL AND revoked_at IS NULL at :83

From backend/config.py:94-100 (dual-secret rotation block):
- webhook_hmac_secret: str = ""
- webhook_hmac_secret_prev: str = ""
- Comments explain rotation runbook in docs/deploy.md

From backend/main.py:74-78 (FastAPI app constructor):
- app = FastAPI(title="...", version="0.1.0", lifespan=lifespan)

From backend/main.py:115-128 (router registration block, expected order):
- app.include_router(auth_router)
- app.include_router(pdb_router)
- app.include_router(agent_router)
- app.include_router(billing_router)
- app.include_router(webhooks_router)
- app.include_router(jobs_router)
- app.include_router(sessions_router)
- app.include_router(user_router)
- app.include_router(admin_router)

From backend/jobs/router.py:32 (APIRouter constructor pattern, the one-line edit target):
- router = APIRouter(prefix="/jobs", tags=["jobs"])
- (to become: APIRouter(prefix="/jobs", tags=["jobs"], include_in_schema=False))

From backend/tests/conftest.py:31-42 (anyio + ASGITransport fixture pattern):
- @pytest.fixture
  def anyio_backend(): return "asyncio"
- @pytest.fixture
  async def client():
      transport = ASGITransport(app=app)
      async with AsyncClient(transport=transport, base_url="http://test") as ac:
          yield ac

From backend/tests/jobs/test_cancel.py:21-25 (dependency_override factory):
- def _override_user(user_id: str = "user-abc"):
      async def _dep(): return user_id
      return _dep
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md + ROADMAP.md + config.py edits</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    backend/config.py
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — must preserve existing v1+v2 sections; you are appending an "API (Phase 13)" section + updating PLAT-V2-01 + traceability table)
    - .planning/ROADMAP.md (line ~262 contains the `pip install kendrew` typo — search and confirm exact text before edit)
    - backend/config.py (current state of Settings class — must understand existing structure before adding new fields)
    - .planning/phases/13-public-api/13-RESEARCH.md §3 (verbatim API-01..API-12 descriptions to mint)
    - .planning/phases/13-public-api/13-PATTERNS.md "config.py (modify - add settings block)" section (dual-secret rotation pattern at backend/config.py:94-100)
  </read_first>
  <action>
    Open REQUIREMENTS.md. Add a new section "### API (Phase 13)" with the 12 IDs (API-01 through API-12) per RESEARCH §3 verbatim. Use checkbox `- [ ]` syntax matching existing AUTH-01..04 / INPUT-01..05 style. Update the v2 entry PLAT-V2-01 to mark it as `Validated by Phase 13 (API-04..API-12)` — keep it in the v2 section but add the validation note. Update the Traceability table with 12 new rows (`| API-01 | Phase 13 | Planned |`, etc.) plus update `PLAT-V2-01` to `| PLAT-V2-01 | Phase 13 | Validated |`.

    Update the Coverage footer (REQUIREMENTS.md lines 155-158). Current verbatim baseline (confirmed by reading REQUIREMENTS.md lines 155-158): line 156 reads `- v1 requirements: 32 total (24 + 8 organizations) plus 7 testing`; line 157 reads `- Mapped to phases: 39`. Phase 12 already promoted ORG-01..ORG-08 into the baseline — do NOT touch the ORG numbers. Replace line 156 with `- v1 requirements: 44 total (24 + 8 organizations + 12 API) plus 7 testing`; replace line 157 with `- Mapped to phases: 51`. (39 + 12 new API rows = 51.)

    Open ROADMAP.md. Find Phase 13 SC 6 line. Confirmed exact pre-edit text: `6. Python SDK published to PyPI: \`pip install kendrew\` with typed client`. Replace `pip install kendrew` with `pip install bindwave` (per D-09 + CONTEXT.md `:208`). This mirrors the Phase 11 SC 6 / SC 8 correction pattern (commit pattern from Phase 11-05).

    Open backend/config.py. Locate the existing block at lines 94-100 (webhook_hmac_secret / webhook_hmac_secret_prev). Add the 4 new Phase 13 fields immediately AFTER that block, with the same docstring style, mirroring PATTERNS.md "config.py (modify - add settings block)": `api_key_pepper: str = ""`, `api_key_pepper_prev: str = ""`, `api_v1_rate_limit: str = "60/minute"`, `idempotency_ttl_hours: int = 25`. Each field gets a 1-3 line comment explaining purpose and pointing to RESEARCH §2.10 / §2.4 / §2.9 / §2.1. Do NOT touch any other field. Do NOT add bcrypt to backend/requirements.txt — RESEARCH §2.10 uses stdlib hmac+hashlib.
  </action>
  <verify>
    <automated>
      grep -c "^- \[ \] \*\*API-" .planning/REQUIREMENTS.md  # must equal 12
      grep "pip install bindwave" .planning/ROADMAP.md  # must match
      ! grep "pip install kendrew" .planning/ROADMAP.md  # must NOT match
      grep -F "v1 requirements: 44 total (24 + 8 organizations + 12 API) plus 7 testing" .planning/REQUIREMENTS.md
      grep -F "Mapped to phases: 51" .planning/REQUIREMENTS.md
      ! grep -F "v1 requirements: 32 total" .planning/REQUIREMENTS.md  # old line must be gone
      ! grep -F "Mapped to phases: 39" .planning/REQUIREMENTS.md  # old count must be gone
      grep -E "^api_key_pepper(_prev)?: str" backend/config.py  # must show 2 lines
      grep -E "^api_v1_rate_limit: str" backend/config.py
      grep -E "^idempotency_ttl_hours: int" backend/config.py
      cd backend && python -c "from config import settings; assert hasattr(settings, 'api_key_pepper'); assert hasattr(settings, 'api_key_pepper_prev'); assert settings.api_v1_rate_limit == '60/minute'; assert settings.idempotency_ttl_hours == 25"
    </automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains 12 new `- [ ] **API-XX**:` lines (API-01 through API-12) under a new "### API (Phase 13)" subheading
    - .planning/REQUIREMENTS.md traceability table includes 12 new rows mapping API-01..API-12 to "Phase 13 | Planned" + 1 updated row for PLAT-V2-01 mapped to "Phase 13 | Validated"
    - .planning/REQUIREMENTS.md coverage footer line 156 reads `- v1 requirements: 44 total (24 + 8 organizations + 12 API) plus 7 testing` (replacing the prior `32 total` line)
    - .planning/REQUIREMENTS.md coverage footer line 157 reads `- Mapped to phases: 51` (replacing the prior `39`)
    - .planning/ROADMAP.md line containing Phase 13 SC 6 now reads `pip install bindwave` (no `kendrew`)
    - `grep "pip install kendrew" .planning/ROADMAP.md` returns nothing
    - backend/config.py has exactly 4 new fields immediately after webhook_hmac_secret_prev: api_key_pepper, api_key_pepper_prev, api_v1_rate_limit, idempotency_ttl_hours, in that order, with matching docstring style
    - `python -c "from config import settings; print(settings.api_v1_rate_limit)"` from inside backend/ prints `60/minute`
    - No other field in config.py is modified (diff is purely additive)
  </acceptance_criteria>
  <done>Three files edited and committed-ready. RESEARCH §3 IDs minted verbatim. D-09 ROADMAP correction applied. Pydantic Settings exposes the 4 new fields and imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations + main.py + 12 router include_in_schema=False flips</name>
  <files>
    supabase/migrations/20260607000001_api_keys.sql
    supabase/migrations/20260607000002_api_key_idempotency.sql
    backend/main.py
    backend/auth/router.py
    backend/agent/router.py
    backend/admin/router.py
    backend/billing/router.py
    backend/debug_routes.py
    backend/jobs/router.py
    backend/organizations/router.py
    backend/pdb_utils/router.py
    backend/sessions/router.py
    backend/user/router.py
    backend/webhooks/router.py
  </files>
  <read_first>
    - supabase/migrations/20260420000001_job_tier_and_budget.sql (analog migration shape — CREATE TABLE, partial index, COMMENT ON COLUMN)
    - supabase/migrations/20260605000001_organizations.sql:18 (public.org_role ENUM definition), :27-36 (PK + ON DELETE CASCADE), :83 (partial index pattern)
    - backend/main.py (full file — must see exact router registration block at lines 115-128 + /health route at 134-163 + app constructor at 74-78 + the CSRFMiddleware call site at 96-104; this plan only edits the app constructor + the /health include_in_schema flag; CSRF exempt_urls is owned by Plan 13-02)
    - backend/jobs/router.py:32 (exact APIRouter constructor line to use as the edit template)
    - backend/auth/router.py:21, backend/agent/router.py:44, backend/admin/router.py:32, backend/billing/router.py:24, backend/debug_routes.py:11, backend/organizations/router.py:41-42 (TWO routers in same file), backend/pdb_utils/router.py:25, backend/sessions/router.py:32, backend/user/router.py:30 (or :31), backend/webhooks/router.py:40 — confirm each APIRouter() constructor line BEFORE editing
    - .planning/phases/13-public-api/13-RESEARCH.md §2.2 (api_keys schema lines 53-68), §2.9 (idempotency lifecycle table lines 315-326), §2.8 (the 12-router table)
    - .planning/phases/13-public-api/13-PATTERNS.md "All 12 legacy routers" section (the 12-row edit table)
  </read_first>
  <action>
    Create supabase/migrations/20260607000001_api_keys.sql per RESEARCH §2.2 lines 53-68 verbatim: CREATE TABLE public.api_keys with columns id (UUID PK gen_random_uuid()), organization_id (UUID NOT NULL REFERENCES public.organizations(id) ON DELETE CASCADE), created_by_user_id (UUID NOT NULL REFERENCES public.users(id) ON DELETE SET NULL), name (TEXT NOT NULL with CHECK name_not_blank), prefix (TEXT NOT NULL), bcrypt_hash (TEXT NOT NULL), role_at_creation (public.org_role NOT NULL), created_at (TIMESTAMPTZ NOT NULL DEFAULT now()), last_used_at (TIMESTAMPTZ), revoked_at (TIMESTAMPTZ). Two partial indexes: idx_api_keys_org ON (organization_id) WHERE revoked_at IS NULL; idx_api_keys_prefix ON (prefix) WHERE revoked_at IS NULL. Add COMMENT ON COLUMN public.api_keys.bcrypt_hash IS 'HMAC-SHA256 (hex) of the plaintext key + server-side pepper. Column name retained from D-03 spec for compatibility; algorithm changed per Phase 13 RESEARCH item 2.10 (high-entropy tokens do not need a slow hash).'

    Create supabase/migrations/20260607000002_api_key_idempotency.sql per RESEARCH §2.9 lines 315-326: CREATE TABLE public.api_key_idempotency with composite PRIMARY KEY (api_key_id, idempotency_key); columns api_key_id (UUID NOT NULL REFERENCES public.api_keys(id) ON DELETE CASCADE), idempotency_key (TEXT NOT NULL), request_body_hash (TEXT NOT NULL), status (TEXT NOT NULL DEFAULT 'pending' with CHECK status IN ('pending', 'completed')), response_status (INT NULL), response_body (JSONB NULL), created_at (TIMESTAMPTZ NOT NULL DEFAULT now()), completed_at (TIMESTAMPTZ NULL). Add CREATE INDEX idx_api_key_idem_created ON public.api_key_idempotency(created_at) to support the reaper cron.

    Open backend/main.py. Make EXACTLY two surgical edits and nothing else:
    (a) Change `FastAPI(title="Kendrew.AI", ...)` constructor at lines 74-78 to `FastAPI(title="Bindwave Public API", version="0.1.0", docs_url="/api/docs", openapi_url="/api/openapi.json", lifespan=lifespan)` per PATTERNS.md adaptation.
    (b) On the `@app.get("/health")` decorator at line ~134, add `include_in_schema=False` kwarg: `@app.get("/health", include_in_schema=False)`.

    Explicitly do NOT touch any other part of main.py. CSRF exempt_urls is owned by Plan 13-02 (auth-primitives plan); the v1 router include + RFC 7807 exception handlers are owned by Plan 13-03. Do NOT add or remove any `app.include_router(...)` calls in this plan.

    Open EACH of the 12 router files. Locate the single `APIRouter(prefix=..., tags=[...])` constructor line at the line numbers cited above. Add `, include_in_schema=False` as the LAST kwarg. For backend/organizations/router.py specifically: it exports TWO routers (the /organizations router at :41 and the /invitations router at :42) — flip BOTH. For backend/debug_routes.py: the flip is independent of the existing `if settings.debug or settings.testing` gate at main.py:140 — keep that gate, just add `include_in_schema=False` to the router constructor at :11. No other edits to any router file.
  </action>
  <verify>
    <automated>
      test -f supabase/migrations/20260607000001_api_keys.sql
      test -f supabase/migrations/20260607000002_api_key_idempotency.sql
      grep -c "CREATE TABLE public.api_keys" supabase/migrations/20260607000001_api_keys.sql  # must be 1
      grep -c "PRIMARY KEY (api_key_id, idempotency_key)" supabase/migrations/20260607000002_api_key_idempotency.sql  # must be 1
      # main.py edits — defensive bounds (no regression of include_in_schema=True; constructor kwargs all present; no spurious router includes added by this plan)
      ! grep -q "include_in_schema=True" backend/main.py  # zero positive cases allowed
      grep -E '^app = FastAPI\(.*title="Bindwave Public API".*version="0\.1\.0".*docs_url="/api/docs".*openapi_url="/api/openapi.json".*lifespan=lifespan' backend/main.py  # all 5 kwargs present on the constructor line
      grep -E '@app\.get\("/health", include_in_schema=False\)' backend/main.py  # /health decorator carries the flag
      MAIN_PRE=$(git show HEAD:backend/main.py | grep -c "^app\.include_router")
      MAIN_POST=$(grep -c "^app\.include_router" backend/main.py)
      [ "$MAIN_PRE" = "$MAIN_POST" ]  # this plan must NOT add/remove router includes (13-02 + 13-03 own that)
      grep "Bindwave Public API" backend/main.py
      grep "docs_url=\"/api/docs\"" backend/main.py
      grep -l "include_in_schema=False" backend/auth/router.py backend/agent/router.py backend/admin/router.py backend/billing/router.py backend/debug_routes.py backend/jobs/router.py backend/organizations/router.py backend/pdb_utils/router.py backend/sessions/router.py backend/user/router.py backend/webhooks/router.py | wc -l  # must be 11 (organizations counts once even with 2 routers)
      grep -c "include_in_schema=False" backend/organizations/router.py  # must be >= 2 (two routers in one file)
    </automated>
  </verify>
  <acceptance_criteria>
    - supabase/migrations/20260607000001_api_keys.sql contains a single CREATE TABLE public.api_keys statement with exactly 10 columns + 2 partial indexes + 1 COMMENT ON COLUMN
    - The api_keys schema CHECK constraint `name_not_blank` is present and references `length(btrim(name)) > 0`
    - supabase/migrations/20260607000002_api_key_idempotency.sql declares PRIMARY KEY (api_key_id, idempotency_key) as a composite key and includes a status column with CHECK ... IN ('pending','completed')
    - backend/main.py FastAPI constructor line contains ALL FOUR kwargs (title="Bindwave Public API", version="0.1.0", docs_url="/api/docs", openapi_url="/api/openapi.json") plus lifespan=lifespan
    - backend/main.py @app.get("/health", include_in_schema=False) appears verbatim — no `include_in_schema=True` regression anywhere in main.py
    - backend/main.py `app.include_router(...)` count is byte-identical pre/post this plan (this plan does NOT add the /api/v1 router include — that lands in Plan 13-03)
    - EVERY ONE of these 11 files contains the substring `include_in_schema=False`: backend/auth/router.py, backend/agent/router.py, backend/admin/router.py, backend/billing/router.py, backend/debug_routes.py, backend/jobs/router.py, backend/organizations/router.py (must contain it AT LEAST TWICE — two routers), backend/pdb_utils/router.py, backend/sessions/router.py, backend/user/router.py, backend/webhooks/router.py
    - Migrations parse with `psql --dry-run` equivalent: `python -c "import sqlglot; sqlglot.parse_one(open('supabase/migrations/20260607000001_api_keys.sql').read())"` succeeds (if sqlglot unavailable, skip this micro-check)
  </acceptance_criteria>
  <done>Two migration SQL files written; main.py app metadata + /health flag updated; 11 router files (12 routers) carry include_in_schema=False; the published OpenAPI surface will go from 100+ paths down to whatever main.py mounts as visible (which is currently zero `/api/v1/*` until 13-03 lands the router include).</done>
</task>

<task type="auto">
  <name>Task 3: Test scaffolds (backend/tests/api_v1/ + backend/tests/contract/) + snapshot fixture</name>
  <files>
    backend/tests/api_v1/__init__.py
    backend/tests/api_v1/conftest.py
    backend/tests/api_v1/test_api_keys.py
    backend/tests/api_v1/test_auth.py
    backend/tests/api_v1/test_idempotency.py
    backend/tests/api_v1/test_pagination.py
    backend/tests/api_v1/test_cursor.py
    backend/tests/api_v1/test_jobs_get.py
    backend/tests/api_v1/test_errors.py
    backend/tests/api_v1/test_rate_limit.py
    backend/tests/contract/__init__.py
    backend/tests/contract/test_openapi_snapshot.py
    backend/tests/contract/test_routers_hidden.py
    backend/tests/contract/_openapi_paths_snapshot.txt
  </files>
  <read_first>
    - backend/tests/conftest.py:31-84 (anyio + ASGITransport fixture pattern — the analog for conftest.py)
    - backend/tests/jobs/test_cancel.py:21-25 (dep override factory pattern)
    - backend/tests/jobs/test_cancel.py:36-67 (multi-acquire pool mock pattern — the analog for test_idempotency.py)
    - backend/tests/jobs/test_cancel.py:82-120 (dependency_overrides + ASGITransport integration test pattern)
    - backend/tests/middleware/test_rate_limit.py:56-100 (pure-function unit test pattern — analog for test_cursor.py + test_routers_hidden.py)
    - .planning/phases/13-public-api/13-VALIDATION.md (per-task test map — confirms what each file must stub)
    - .planning/phases/13-public-api/13-PATTERNS.md "test_api_keys.py", "test_idempotency.py", "test_pagination.py + test_cursor.py", "test_rate_limit.py", "test_errors.py" sections (the exact test cases each file must contain as stubs)
  </read_first>
  <action>
    Create `backend/tests/api_v1/__init__.py` as empty package marker (same shape as backend/tests/jobs/__init__.py).

    Create `backend/tests/api_v1/conftest.py` with three fixtures per PATTERNS.md "conftest.py" section. (a) `synthetic_api_key` returns `(plaintext, prefix, hash, row_dict)` 4-tuple; `row_dict` uses zero-UUIDs `00000000-0000-0000-0000-000000000001` (api_key id) and `...000002` (organization_id), role_at_creation='owner', revoked_at=None. (b) `idempotency_key` returns `uuid.uuid4().hex`. (c) `override_api_key` does `app.dependency_overrides[get_current_api_key] = lambda: (row["organization_id"], "owner")` inside a yield + cleanup. Use the SAME `anyio_backend` + `client` fixtures shape as backend/tests/conftest.py:31-42 with `ASGITransport(app=app)` + base_url="http://test". The synthetic key calls `from auth.api_keys import generate_api_key` — that module does NOT exist yet (Plan 13-02 ships it); guard the import inside a try/except ImportError and have the fixture pytest.skip with message "Plan 13-02 must ship auth.api_keys" when missing so the scaffold is committable now.

    Create `backend/tests/api_v1/test_api_keys.py` with STUBS for: test_create_returns_plaintext, test_revoked_key_rejects, test_generate_api_key_format, test_verify_api_key_constant_time, test_pepper_rotation. Every stub is `pytest.skip("Pending: Plan 13-04 ships web create endpoint")` (or the appropriate downstream plan). Each stub has a single-line docstring linking to the API-XX requirement.

    Create `backend/tests/api_v1/test_auth.py` with stub test_returns_org_role_tuple (API-02).

    Create `backend/tests/api_v1/test_idempotency.py` with stubs test_replay, test_body_mismatch_returns_422, test_pending_returns_409 (API-04).

    Create `backend/tests/api_v1/test_pagination.py` with stub test_cursor_stable_under_insert (API-05).

    Create `backend/tests/api_v1/test_cursor.py` with stubs test_round_trip, test_garbage_input (API-05) — use the pure-function pattern from middleware/test_rate_limit.py. Stubs may import from `api.v1.cursor` inside try/except (Plan 13-03 ships it).

    Create `backend/tests/api_v1/test_jobs_get.py` with stub test_get_returns_inline_presigned_urls (API-06).

    Create `backend/tests/api_v1/test_errors.py` with stubs test_problem_json, test_validation_error_problem_json, test_web_flow_regression (API-07).

    Create `backend/tests/api_v1/test_rate_limit.py` with stubs test_60rpm, test_headers_on_200, test_headers_on_429 (API-08, API-10).

    Create `backend/tests/contract/__init__.py` empty marker.

    Create `backend/tests/contract/test_openapi_snapshot.py` ACTIVE (not stubbed) — verbatim per PATTERNS.md "test_openapi_snapshot.py" section: load `from main import app`, dump `sorted(spec["paths"].keys())`, read fixture file at `backend/tests/contract/_openapi_paths_snapshot.txt`, assert equality with explicit error message. The test reads the fixture as a list, skipping comment lines starting with `#` and blank lines, so the initial empty-modulo-comment file matches the initial empty spec.

    Create `backend/tests/contract/test_routers_hidden.py` ACTIVE — iterate `app.routes`, assert any route whose `route.path` does not start with `/api/v1/` AND does not start with `/health` (the /health is also hidden but per RESEARCH §5.9 the snapshot test handles it; this test focuses on legacy ROUTERS specifically) — confirm `route.include_in_schema is False` for every route whose `tags` include any of `auth|agent|admin|billing|debug|jobs|organizations|invitations|pdb|sessions|user|webhooks`.

    Create `backend/tests/contract/_openapi_paths_snapshot.txt`. Per PATTERNS.md "_openapi_paths_snapshot.txt" section, the initial snapshot lists ONLY /api/v1/* paths because Wave 0 also flips include_in_schema=False on all legacy routers in the same wave. Initial content (one path per line, alphabetically sorted): EMPTY (no `/api/v1/*` paths exist yet because Plan 13-03 hasn't added them). Add a single comment header line `# Phase 13 OpenAPI surface snapshot — regenerate via Plan 13-07 after 13-03/13-04 land routes`. Plan 13-07 will overwrite this file with the final post-D-15 surface (5-6 `/api/v1/*` paths). For NOW, the snapshot test passes because both `sorted(spec["paths"].keys())` is empty (no /api/v1 router mounted yet) AND the fixture body (excluding comments) is empty.
  </action>
  <verify>
    <automated>
      test -f backend/tests/api_v1/__init__.py
      test -f backend/tests/api_v1/conftest.py
      ls backend/tests/api_v1/test_*.py | wc -l  # must be >= 8
      test -f backend/tests/contract/__init__.py
      test -f backend/tests/contract/test_openapi_snapshot.py
      test -f backend/tests/contract/test_routers_hidden.py
      test -f backend/tests/contract/_openapi_paths_snapshot.txt
      cd backend && pytest tests/api_v1 tests/contract --collect-only -q  # all stubs collect cleanly (skipped or runnable)
      cd backend && pytest tests/contract/test_openapi_snapshot.py -x  # must PASS (empty spec == empty fixture)
      cd backend && pytest tests/contract/test_routers_hidden.py -x  # must PASS (all 12 routers flipped)
    </automated>
  </verify>
  <acceptance_criteria>
    - All 14 listed files exist
    - `pytest backend/tests/api_v1 --collect-only -q` reports >= 14 test functions collected (skipped is fine)
    - `pytest backend/tests/contract/test_openapi_snapshot.py -x` exits 0 (passes — spec paths and fixture both effectively empty modulo comments)
    - `pytest backend/tests/contract/test_routers_hidden.py -x` exits 0 (passes — all 12 routers carry include_in_schema=False after Task 2)
    - backend/tests/api_v1/conftest.py exposes fixtures `synthetic_api_key`, `idempotency_key`, `override_api_key` discoverable via `pytest --fixtures backend/tests/api_v1/`
    - No stub raises a hard ImportError (downstream-module imports are guarded with try/except + pytest.skip)
  </acceptance_criteria>
  <done>backend/tests/api_v1/ + backend/tests/contract/ scaffolds exist. The two ACTIVE contract tests pass green NOW because the surface is empty and all routers are flipped. The 8 API-v1 test files have stubs ready for downstream plans to fill in. Conftest fixtures wire to the (not-yet-existing) auth.api_keys module via lazy import guards.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 [BLOCKING]: Push migrations to live Supabase via supabase db push</name>
  <what-built>Two new migrations: 20260607000001_api_keys.sql + 20260607000002_api_key_idempotency.sql</what-built>
  <why-blocking>The phase verification depends on api_keys + api_key_idempotency tables existing in the live DB. Without push, Plan 13-02 onwards passes type-checks against Pydantic but every integration test that touches Postgres fails because the tables don't exist. Build/type checks do not catch this (types come from Pydantic models, not the live DB).</why-blocking>
  <how-to-verify>
    1. From repo root run: `SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN supabase db push` (Windows PowerShell: set env var via `$env:SUPABASE_ACCESS_TOKEN=$env:SUPABASE_ACCESS_TOKEN` then run `supabase db push`).
    2. Confirm the push reports applying exactly 2 new migrations: `20260607000001_api_keys.sql` and `20260607000002_api_key_idempotency.sql`.
    3. Verify table structure via Supabase Studio SQL editor OR via `psql` against the live project:
       `\d public.api_keys` must show 10 columns including bcrypt_hash, prefix, role_at_creation, organization_id, revoked_at, last_used_at, created_at.
       `\d public.api_key_idempotency` must show composite PK (api_key_id, idempotency_key) and a status column with CHECK constraint.
    4. Verify the two partial indexes exist: `SELECT indexname FROM pg_indexes WHERE tablename = 'api_keys'` must return idx_api_keys_org AND idx_api_keys_prefix.
    5. Verify the foreign-key references resolve: `SELECT conname FROM pg_constraint WHERE conrelid = 'public.api_keys'::regclass` must include CASCADE to public.organizations and SET NULL to public.users.
  </how-to-verify>
  <resume-signal>Type "applied" once the push succeeds and the table checks pass. If push fails with an interactive prompt that can't be suppressed, abort and re-run with `--linked` or `--password` flag per Supabase CLI docs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration deployment | Supabase CLI runs SQL with privileged role; misconfigured grant could leak data. Mitigated by RLS already in place from Phase 12 on organizations table; api_keys inherits the same FK + cascade. |
| OpenAPI publish | The published spec at `/api/openapi.json` becomes the public-API contract. Any accidentally-visible legacy route leaks internal endpoints. |
| New Pydantic Settings fields | api_key_pepper is a server-side secret loaded from env. If left empty in production, key verification silently falls through dual-secret loop returning False on every verify. Plan 13-02 surfaces this; this plan defers detection to integration tests in 13-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Spoofing | Webhook spec leak | mitigate | webhooks/router.py gets include_in_schema=False (Task 2); /webhooks/* path stays out of /api/openapi.json so HMAC scheme is not advertised |
| T-13-09 | Tampering | api_keys table created with privileged migration | mitigate | Same migration shape + FK cascade as existing organizations table; Supabase RLS continues to gate row-level access via the api_key_id-keyed bucket in 13-03 |
| T-13-09 | Tampering | Pepper rotation grace window | mitigate | Pydantic Settings exposes api_key_pepper + api_key_pepper_prev mirroring webhook_hmac_secret pattern; rotation runbook lives alongside webhook rotation in docs/deploy.md (added in Plan 13-07) |
| T-13-09 | Information disclosure | Empty pepper allows verify_api_key to fall through | accept | Plan 13-02 ships the verify code path; this plan only declares the settings field. Empty-string default + integration test in 13-03 ensures startup catches an unset pepper in prod (RESEARCH Open Q2). |

(T-13-01..07 covered by downstream plans 13-02 / 13-03 / 13-04.)
</threat_model>

<verification>
## Pre-flight (RESEARCH §5.11, §5.14 — Phase 12 cutover precondition)

Before merging this plan to main, manually confirm:
- [ ] `settings.organizations_enabled = True` in production env (Phase 12 runbook step landed)
- [ ] supabase/migrations/20260606000001_drop_users_stripe_customer_id.sql has been applied (Phase 12 runbook step 9) — `\d public.users` no longer shows stripe_customer_id column
- [ ] `public.organizations` table exists with a `stripe_customer_id` column (Phase 12 migration that this plan FKs into)
- [ ] `public.org_role` ENUM exists with values {owner, admin, member}

If any precondition fails, defer this plan and complete the Phase 12 cutover runbook first.

## Wave 0 close-out checks (run after all 4 tasks complete)

- All routers + /health route hidden from /openapi.json (Task 2 verify covers)
- 4 new Pydantic Settings fields load (Task 1 verify covers)
- 2 migrations exist in repo AND applied to live DB (Task 4 [BLOCKING] verify covers)
- Test scaffolds for api_v1 + contract directories collect (Task 3 verify covers)
- ROADMAP correction `kendrew → bindwave` landed (Task 1 verify covers)
- REQUIREMENTS.md mints API-01..API-12 + promotes PLAT-V2-01 (Task 1 verify covers)
</verification>

<success_criteria>
1. REQUIREMENTS.md exposes API-01..API-12 with descriptions matching RESEARCH §3 verbatim
2. REQUIREMENTS.md PLAT-V2-01 row in traceability shows "Phase 13 | Validated"
3. ROADMAP.md Phase 13 SC 6 reads `pip install bindwave` (no `kendrew`)
4. 4 new Pydantic Settings fields callable as `settings.api_key_pepper`, `settings.api_key_pepper_prev`, `settings.api_v1_rate_limit`, `settings.idempotency_ttl_hours` with documented defaults
5. supabase db push applies 2 new migrations; tables and indexes verifiable via Supabase Studio
6. `curl -s http://localhost:8000/openapi.json | python -c "import sys,json; spec=json.load(sys.stdin); assert all(p.startswith('/api/v1/') for p in spec['paths'].keys()), spec['paths'].keys()"` succeeds — confirms only `/api/v1/*` would appear, but spec is currently EMPTY because Plan 13-03 mounts the router. (Net effect: 0 visible routes after this plan; the constraint is "no LEGACY route is visible".)
7. backend/tests/contract/test_openapi_snapshot.py + test_routers_hidden.py both pass
8. `pytest backend/tests --collect-only -q` reports the new test files without import errors
</success_criteria>

<output>
Create `.planning/phases/13-public-api/13-01-SUMMARY.md` when done.
</output>
</content>
</invoke>