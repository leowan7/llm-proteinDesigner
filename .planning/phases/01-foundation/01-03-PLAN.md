---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  # NOTE: 19 files listed but most are generated by `npm create vite` and `npx shadcn add`
  # CLI tools. Actual hand-written files are ~5. No split required.
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/globals.css
  - frontend/src/lib/utils.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.test.ts
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/label.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/form.tsx
  - frontend/src/components/auth/AuthLayout.tsx
  - frontend/tailwind.config.ts
  - frontend/components.json
autonomous: true
requirements:
  - AUTH-04
must_haves:
  truths:
    - "npm run dev starts Vite dev server on http://localhost:5173 without errors"
    - "shadcn components (Button, Input, Label, Card, Form) are installed and importable"
    - "Dark theme with indigo accent is applied globally"
    - "AuthLayout component centers a Card on a dark background"
    - "API client sends requests to FastAPI with credentials: include"
    - "npx vitest run passes with at least one smoke test"
  artifacts:
    - path: "frontend/package.json"
      provides: "Vite + React + TypeScript project with shadcn dependencies and vitest"
      contains: "react"
    - path: "frontend/src/globals.css"
      provides: "Dark theme CSS variables with indigo-500 primary override"
      contains: "--primary: 238.7 83.5% 66.7%"
    - path: "frontend/src/lib/api.ts"
      provides: "API client for FastAPI backend with cookie credentials"
      contains: "credentials"
    - path: "frontend/src/lib/api.test.ts"
      provides: "Smoke test for API client module"
      contains: "test"
    - path: "frontend/src/components/auth/AuthLayout.tsx"
      provides: "Shared auth page layout with centered Card"
      contains: "Card"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "http://localhost:8000"
      via: "fetch with credentials: include"
      pattern: "credentials.*include"
    - from: "frontend/src/globals.css"
      to: "tailwind.config.ts"
      via: "CSS variables consumed by Tailwind classes"
      pattern: "--primary"
---

<objective>
Scaffold the React + TypeScript frontend with Vite, install shadcn/ui components, apply the dark theme with indigo accent, create the shared AuthLayout component, build the API client that communicates with FastAPI using cookie credentials, and set up vitest with a smoke test.

Purpose: Provide the frontend foundation that Plan 04 will build the 6 auth screens on. Separating scaffold from screens keeps each plan within context budget.

Output: A running Vite dev server with shadcn components, dark theme, AuthLayout, API client, and vitest configured with a passing smoke test.
</objective>

<execution_context>
@C:/Users/lab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vite + React + TypeScript scaffold with shadcn/ui, dark theme, and vitest</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/index.html, frontend/src/main.tsx, frontend/src/App.tsx, frontend/src/index.css, frontend/src/globals.css, frontend/tailwind.config.ts, frontend/components.json, frontend/src/lib/utils.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json</files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
1. Scaffold the Vite project:
```bash
cd C:/Users/lab/Documents/Claude_projects/llm-proteinDesigner
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
```

2. Install Tailwind CSS (v4 or v3 depending on Vite template -- use v3 for shadcn compatibility):
```bash
npm install -D tailwindcss@3 postcss autoprefixer
npx tailwindcss init -p
```

3. Install shadcn/ui dependencies:
```bash
npm install tailwindcss-animate class-variance-authority clsx tailwind-merge lucide-react
npm install @radix-ui/react-label @radix-ui/react-slot
npm install react-hook-form @hookform/resolvers zod
npm install @fontsource/inter
```

4. Initialize shadcn:
```bash
npx shadcn@latest init
# TypeScript: yes, Style: Default, Base color: Zinc, CSS variables: yes
```

5. Add shadcn components:
```bash
npx shadcn@latest add button input label card form
```

6. Install react-router-dom for routing:
```bash
npm install react-router-dom
```

7. Install vitest and testing-library for frontend tests:
```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

8. Update `frontend/vite.config.ts` to include vitest config:

```ts
/// <reference types="vitest" />
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import path from "path";

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  test: {
    globals: true,
    environment: "jsdom",
    setupFiles: [],
  },
});
```

9. Edit `frontend/src/globals.css` (created by shadcn init) to override `--primary` with indigo-500 in the dark theme block. Find the `.dark` or `:root` dark section and set:

```css
--primary: 238.7 83.5% 66.7%;          /* indigo-500 */
--primary-foreground: 0 0% 100%;        /* white text on indigo */
```

Also ensure `--ring` uses the same indigo value:
```css
--ring: 238.7 83.5% 66.7%;             /* indigo-500 focus ring */
```

10. Edit `frontend/src/main.tsx` to import Inter font and globals:

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import "@fontsource/inter/400.css";
import "@fontsource/inter/600.css";
import "./globals.css";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

11. Edit `frontend/src/App.tsx` to set up react-router with a placeholder route:

```tsx
import { BrowserRouter, Routes, Route } from "react-router-dom";

function App() {
  return (
    <BrowserRouter>
      <div className="dark min-h-screen bg-background font-sans text-foreground antialiased">
        <Routes>
          <Route path="/" element={<div className="flex items-center justify-center min-h-screen"><p className="text-muted-foreground">LLM Protein Designer</p></div>} />
        </Routes>
      </div>
    </BrowserRouter>
  );
}

export default App;
```

12. Ensure `frontend/index.html` has `<html lang="en" class="dark">` so the dark theme applies globally.

13. Update `frontend/tailwind.config.ts` to include the Inter font family in the theme:

```ts
// In theme.extend.fontFamily:
fontFamily: {
  sans: ["Inter", "system-ui", "sans-serif"],
},
```
  </action>
  <verify>
    <automated>cd C:/Users/lab/Documents/Claude_projects/llm-proteinDesigner/frontend && test -f package.json && grep -q "react" package.json && grep -q "react-router-dom" package.json && grep -q "lucide-react" package.json && grep -q "@fontsource/inter" package.json && grep -q "vitest" package.json && test -f src/globals.css && grep -q "238.7 83.5% 66.7%" src/globals.css && test -f components.json && test -f src/components/ui/button.tsx && test -f src/components/ui/input.tsx && test -f src/components/ui/card.tsx && test -f src/components/ui/form.tsx && npx tsc --noEmit 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains dependencies: react, react-dom, react-router-dom, lucide-react, @fontsource/inter, react-hook-form, zod, @hookform/resolvers
    - frontend/package.json contains devDependencies: vitest, @testing-library/react, @testing-library/jest-dom, jsdom
    - frontend/vite.config.ts contains vitest test configuration with `environment: "jsdom"`
    - frontend/src/globals.css contains `--primary: 238.7 83.5% 66.7%` (indigo-500 override)
    - frontend/src/globals.css contains `--ring: 238.7 83.5% 66.7%` (indigo focus ring)
    - frontend/src/main.tsx imports `@fontsource/inter/400.css` and `@fontsource/inter/600.css`
    - frontend/src/App.tsx contains `BrowserRouter` and `className="dark"`
    - frontend/index.html contains `class="dark"`
    - frontend/components.json exists (shadcn config)
    - frontend/src/components/ui/button.tsx exists
    - frontend/src/components/ui/input.tsx exists
    - frontend/src/components/ui/label.tsx exists
    - frontend/src/components/ui/card.tsx exists
    - frontend/src/components/ui/form.tsx exists
    - frontend/tailwind.config.ts contains `"Inter"` in fontFamily
    - `npx tsc --noEmit` exits without type errors
  </acceptance_criteria>
  <done>Vite + React + TypeScript project running with shadcn/ui components installed, dark theme with indigo accent applied, Inter font loaded at weights 400 and 600, react-router-dom configured, vitest installed and configured</done>
</task>

<task type="auto">
  <name>Task 2: AuthLayout component, API client, and smoke test</name>
  <files>frontend/src/components/auth/AuthLayout.tsx, frontend/src/lib/api.ts, frontend/src/lib/api.test.ts</files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md
    frontend/src/components/ui/card.tsx
    frontend/src/globals.css
  </read_first>
  <action>
1. Create `frontend/src/components/auth/AuthLayout.tsx`:

```tsx
import { Card, CardContent, CardFooter, CardHeader } from "@/components/ui/card";

interface AuthLayoutProps {
  title: string;
  subtitle: string;
  footer?: React.ReactNode;
  children: React.ReactNode;
}

/**
 * Shared layout for all 6 auth screens.
 *
 * Renders a centered Card on the dark background.
 * Layout contract from UI-SPEC:
 * - max-width: 400px, horizontally centered, vertical center with min-h-screen
 * - Card background: --card (zinc-900), border: --border
 * - CardHeader: padding lg (24px) top + horizontal; gap sm (8px) between title and description
 * - CardContent: padding md (16px) horizontal, lg (24px) bottom
 * - CardFooter: padding md (16px), centered text
 *
 * Typography:
 * - Title: Heading (20px/600)
 * - Subtitle: Body (16px/400, muted)
 */
export function AuthLayout({ title, subtitle, footer, children }: AuthLayoutProps) {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background px-4">
      <Card className="w-full max-w-[400px]">
        <CardHeader className="space-y-2 p-6">
          <h1 className="text-xl font-semibold leading-[1.2] tracking-tight">
            {title}
          </h1>
          <p className="text-base font-normal leading-[1.5] text-muted-foreground">
            {subtitle}
          </p>
        </CardHeader>
        <CardContent className="px-6 pb-6">
          {children}
        </CardContent>
        {footer && (
          <CardFooter className="flex flex-col items-center gap-2 px-4 pb-4 pt-0 text-sm text-muted-foreground">
            {footer}
          </CardFooter>
        )}
      </Card>
    </div>
  );
}
```

2. Create `frontend/src/lib/api.ts`:

```ts
/**
 * API client for FastAPI backend.
 *
 * All requests include credentials (cookies) for HTTP-only cookie auth.
 * CSRF: reads csrftoken cookie and sends it in x-csrftoken header on mutating requests.
 * On 401 response, attempts one token refresh before failing.
 */

const API_BASE = "http://localhost:8000";

function getCsrfToken(): string | null {
  const match = document.cookie.match(/(?:^|;\s*)csrftoken=([^;]*)/);
  return match ? decodeURIComponent(match[1]) : null;
}

interface ApiOptions {
  method?: string;
  body?: unknown;
  skipRefreshRetry?: boolean;
}

export class ApiError extends Error {
  constructor(
    public status: number,
    public detail: string,
  ) {
    super(detail);
    this.name = "ApiError";
  }
}

async function refreshToken(): Promise<boolean> {
  try {
    const response = await fetch(`${API_BASE}/auth/refresh`, {
      method: "POST",
      credentials: "include",
      headers: {
        "x-csrftoken": getCsrfToken() || "",
      },
    });
    return response.ok;
  } catch {
    return false;
  }
}

export async function api<T = unknown>(
  path: string,
  options: ApiOptions = {},
): Promise<T> {
  const { method = "GET", body, skipRefreshRetry = false } = options;

  const headers: Record<string, string> = {};

  if (body !== undefined) {
    headers["Content-Type"] = "application/json";
  }

  // Attach CSRF token on mutating requests
  if (method !== "GET" && method !== "HEAD") {
    const csrf = getCsrfToken();
    if (csrf) {
      headers["x-csrftoken"] = csrf;
    }
  }

  const response = await fetch(`${API_BASE}${path}`, {
    method,
    headers,
    body: body !== undefined ? JSON.stringify(body) : undefined,
    credentials: "include",
  });

  // On 401, attempt one silent refresh then retry
  if (response.status === 401 && !skipRefreshRetry) {
    const refreshed = await refreshToken();
    if (refreshed) {
      return api<T>(path, { ...options, skipRefreshRetry: true });
    }
  }

  if (!response.ok) {
    const data = await response.json().catch(() => ({ detail: "Request failed" }));
    throw new ApiError(response.status, data.detail || "Request failed");
  }

  return response.json() as Promise<T>;
}
```

3. Create `frontend/src/lib/api.test.ts` (smoke test for the API client module):

```ts
import { describe, it, expect } from "vitest";
import { ApiError } from "./api";

describe("ApiError", () => {
  it("creates an error with status and detail", () => {
    const error = new ApiError(401, "Not authenticated");
    expect(error.status).toBe(401);
    expect(error.detail).toBe("Not authenticated");
    expect(error.name).toBe("ApiError");
    expect(error.message).toBe("Not authenticated");
  });

  it("is an instance of Error", () => {
    const error = new ApiError(500, "Server error");
    expect(error).toBeInstanceOf(Error);
  });
});
```
  </action>
  <verify>
    <automated>cd C:/Users/lab/Documents/Claude_projects/llm-proteinDesigner/frontend && test -f src/components/auth/AuthLayout.tsx && grep -q "AuthLayout" src/components/auth/AuthLayout.tsx && grep -q "max-w-\[400px\]" src/components/auth/AuthLayout.tsx && grep -q "min-h-screen" src/components/auth/AuthLayout.tsx && grep -q "text-xl font-semibold" src/components/auth/AuthLayout.tsx && test -f src/lib/api.ts && grep -q "credentials.*include" src/lib/api.ts && grep -q "x-csrftoken" src/lib/api.ts && grep -q "refreshToken" src/lib/api.ts && grep -q "API_BASE" src/lib/api.ts && test -f src/lib/api.test.ts && npx vitest run --reporter=verbose 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/auth/AuthLayout.tsx exports `AuthLayout` component
    - AuthLayout renders Card with `max-w-[400px]` and parent `min-h-screen items-center justify-center`
    - AuthLayout title uses `text-xl font-semibold` (20px/600 per UI-SPEC)
    - AuthLayout subtitle uses `text-base font-normal text-muted-foreground` (16px/400 per UI-SPEC)
    - AuthLayout accepts `title`, `subtitle`, `footer`, `children` props
    - frontend/src/lib/api.ts exports `api` function and `ApiError` class
    - api.ts sets `credentials: "include"` on all fetch calls
    - api.ts reads `csrftoken` cookie and sends it as `x-csrftoken` header on non-GET requests
    - api.ts implements silent token refresh on 401 (calls `/auth/refresh` then retries once)
    - frontend/src/lib/api.test.ts exists and tests ApiError construction
    - `npx vitest run` passes with at least 1 test
    - `npx tsc --noEmit` exits without type errors
  </acceptance_criteria>
  <done>AuthLayout component implementing the UI-SPEC layout contract, API client with cookie credentials, CSRF handling, and silent 401 refresh retry, plus a passing vitest smoke test for the API client module</done>
</task>

</tasks>

<verification>
- `npm run dev` in frontend/ starts Vite dev server on port 5173 without errors
- Browser shows dark background with "LLM Protein Designer" text
- shadcn components importable: Button, Input, Label, Card, Form
- globals.css has indigo-500 as --primary
- AuthLayout renders a centered card with correct typography
- api.ts sends credentials: include and handles CSRF
- `npx vitest run` passes with at least 1 test
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
Frontend scaffold is complete with dark theme, indigo accent, shadcn/ui components, AuthLayout, API client, and vitest configured with a passing smoke test. Ready for Plan 04 to build the 6 auth screens.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
