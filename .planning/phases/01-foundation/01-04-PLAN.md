---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - frontend/src/App.tsx
  - frontend/src/pages/SignUp.tsx
  - frontend/src/pages/Login.tsx
  - frontend/src/pages/VerifyEmail.tsx
  - frontend/src/pages/EmailConfirmed.tsx
  - frontend/src/pages/ResetPassword.tsx
  - frontend/src/pages/ResetPasswordConfirm.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
must_haves:
  truths:
    - "User can fill in email and password on /signup and see a success message directing them to check email"
    - "User can sign in with verified credentials on /login and be redirected to the app"
    - "User sees 'Check your inbox' on /verify-email after signup"
    - "User sees 'Email verified' on /email-confirmed after clicking verification link"
    - "User can request a password reset on /reset-password"
    - "User can set a new password on /reset-password/confirm after clicking reset link"
    - "All forms show field-level validation errors (email format, password length, password mismatch)"
    - "Submit buttons show loading spinner during API calls"
    - "Footer links navigate between auth screens correctly"
  artifacts:
    - path: "frontend/src/pages/SignUp.tsx"
      provides: "AUTH-01: Account creation form"
      contains: "Create your account"
    - path: "frontend/src/pages/Login.tsx"
      provides: "AUTH-04: Login form"
      contains: "Sign in"
    - path: "frontend/src/pages/VerifyEmail.tsx"
      provides: "AUTH-02: Email verification pending screen"
      contains: "Check your inbox"
    - path: "frontend/src/pages/EmailConfirmed.tsx"
      provides: "AUTH-02: Email verified confirmation screen"
      contains: "Email verified"
    - path: "frontend/src/pages/ResetPassword.tsx"
      provides: "AUTH-03: Password reset request form"
      contains: "Reset your password"
    - path: "frontend/src/pages/ResetPasswordConfirm.tsx"
      provides: "AUTH-03: New password form"
      contains: "Set a new password"
  key_links:
    - from: "frontend/src/pages/Login.tsx"
      to: "frontend/src/lib/api.ts"
      via: "api('/auth/login', { method: 'POST', body: ... })"
      pattern: "api.*auth/login"
    - from: "frontend/src/pages/SignUp.tsx"
      to: "frontend/src/lib/api.ts"
      via: "api('/auth/signup', { method: 'POST', body: ... })"
      pattern: "api.*auth/signup"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/*.tsx"
      via: "React Router <Route> elements"
      pattern: "Route.*path"
---

<objective>
Build all 6 auth screens using the AuthLayout and API client from Plan 03, wire them to the FastAPI auth endpoints from Plan 02, and verify the complete auth flow end-to-end.

Purpose: This is the user-facing deliverable of Phase 1. After this plan, a user can create an account, verify their email, log in, stay logged in, and reset their password.

Output: Six working auth screens with form validation, loading states, error handling, and navigation between screens.
</objective>

<execution_context>
@C:/Users/lab/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 03: AuthLayout and API client -->

From frontend/src/components/auth/AuthLayout.tsx:
```tsx
interface AuthLayoutProps {
  title: string;
  subtitle: string;
  footer?: React.ReactNode;
  children: React.ReactNode;
}
export function AuthLayout({ title, subtitle, footer, children }: AuthLayoutProps): JSX.Element;
```

From frontend/src/lib/api.ts:
```tsx
export class ApiError extends Error {
  status: number;
  detail: string;
}
export async function api<T = unknown>(path: string, options?: { method?: string; body?: unknown; skipRefreshRetry?: boolean }): Promise<T>;
```

<!-- From Plan 02: Backend auth endpoints -->

POST /auth/signup { email, password } -> { message: "Account created..." }
POST /auth/login { email, password } -> { message: "Logged in", user_id: string } + Set-Cookie
POST /auth/logout -> { message: "Logged out" } + clear cookies
POST /auth/refresh -> { message: "Token refreshed" } + Set-Cookie
POST /auth/reset-password { email } -> { message: "If an account..." }
POST /auth/update-password { password } -> { message: "Password updated..." }
GET /auth/me -> { user_id: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build all 6 auth pages and wire routes</name>
  <files>frontend/src/pages/SignUp.tsx, frontend/src/pages/Login.tsx, frontend/src/pages/VerifyEmail.tsx, frontend/src/pages/EmailConfirmed.tsx, frontend/src/pages/ResetPassword.tsx, frontend/src/pages/ResetPasswordConfirm.tsx, frontend/src/App.tsx</files>
  <read_first>
    frontend/src/components/auth/AuthLayout.tsx
    frontend/src/lib/api.ts
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/form.tsx
    .planning/phases/01-foundation/01-UI-SPEC.md
  </read_first>
  <action>
Build each page following the UI-SPEC copywriting contract exactly. All pages use AuthLayout. All forms use react-hook-form + zod for validation. All submit buttons show Loader2 spinner during API calls. All error messages match the UI-SPEC error copy exactly.

**1. Create `frontend/src/pages/SignUp.tsx`:**
- Title: "Create your account"
- Subtitle: "Enter your email and a password to get started."
- Fields: Email (label "Email", placeholder "you@example.com"), Password (label "Password", placeholder "At least 8 characters"), Confirm password (label "Confirm password", placeholder "Re-enter your password")
- Zod schema: email must be valid email (error: "Enter a valid email address."), password min 8 chars (error: "Password must be at least 8 characters."), confirmPassword must match password (error: "Passwords do not match.")
- CTA: "Create account" button, full-width, height 44px min, primary variant
- Loading state: button shows Loader2 icon + "Creating account..."
- On success: navigate to `/verify-email?email={email}`
- On API error 409: show "An account with this email already exists. Sign in instead."
- On network error: show "Unable to connect. Check your connection and try again."
- Footer: `Already have an account? <Link to="/login">Sign in</Link>`
- API call: `api('/auth/signup', { method: 'POST', body: { email, password } })`

**2. Create `frontend/src/pages/VerifyEmail.tsx`:**
- Title: "Check your inbox"
- Subtitle: "We sent a verification link to {email}. Click it to activate your account." (read email from URL search param `?email=`)
- No form fields
- Secondary CTA: "Resend verification email" (ghost variant button). On click, call `api('/auth/signup', { method: 'POST', body: { email, password: '' } })` -- note: resend is re-triggering signup which resends the verification email in Supabase. If this fails, silently ignore (user can try again). Show a temporary "Verification email resent" message for 3 seconds on success.
- Footer: `<Link to="/login">Back to sign in</Link>`

**3. Create `frontend/src/pages/EmailConfirmed.tsx`:**
- Title: "Email verified"
- Subtitle: "Your account is active. You can now sign in."
- No form fields
- CTA: "Sign in" button (primary variant), navigates to `/login`
- No footer links

**4. Create `frontend/src/pages/Login.tsx`:**
- Title: "Sign in"
- Subtitle: "Welcome back."
- Fields: Email (label "Email", placeholder "you@example.com"), Password (label "Password", placeholder "At least 8 characters")
- Zod schema: email valid email, password min 1 char (just required, no length check on login)
- CTA: "Sign in" button, full-width, height 44px min, primary variant
- Loading state: button shows Loader2 icon + "Signing in..."
- On success: navigate to `/` (home/dashboard -- placeholder for now)
- On API error 401: show "Incorrect email or password."
- On API error 403: show "Verify your email before signing in. Check your inbox or resend the link below." with a link to `/verify-email?email={email}`
- On network error: show "Unable to connect. Check your connection and try again."
- Footer line 1: `Don't have an account? <Link to="/signup">Create one</Link>`
- Footer line 2: `Forgot your password? <Link to="/reset-password">Reset it</Link>`
- API call: `api('/auth/login', { method: 'POST', body: { email, password } })`

**5. Create `frontend/src/pages/ResetPassword.tsx`:**
- Title: "Reset your password"
- Subtitle: "Enter your email and we'll send you a reset link."
- Fields: Email (label "Email", placeholder "you@example.com")
- Zod schema: email valid email
- CTA: "Send reset link" button, full-width, primary variant
- Loading state: Loader2 icon + "Sending..."
- On success: show a success message "If an account with that email exists, a reset link has been sent." and disable the form
- Footer: `<Link to="/login">Back to sign in</Link>`
- API call: `api('/auth/reset-password', { method: 'POST', body: { email } })`

**6. Create `frontend/src/pages/ResetPasswordConfirm.tsx`:**
- Title: "Set a new password"
- Subtitle: "Choose a strong password for your account."
- Fields: New password (label "New password", placeholder "At least 8 characters"), Confirm password (label "Confirm password", placeholder "Re-enter your password")
- Zod schema: password min 8 chars, confirmPassword must match
- CTA: "Set new password" button, full-width, primary variant
- Loading state: Loader2 icon + "Updating..."
- On success: navigate to `/login` with a flash message or just navigate (user sees login page)
- Note: This page is reached after clicking the Supabase reset link, which redirects to the frontend with tokens in the URL hash. The page should extract access_token from the URL hash fragment and set it as a cookie (or pass to the API). Implementation detail: Supabase redirects to `{site_url}/reset-password/confirm#access_token=...&refresh_token=...&type=recovery`. Parse the hash, then call `api('/auth/update-password', { method: 'POST', body: { password } })` -- the backend should have the access_token from the cookie or this page needs to set it first. For simplicity, this page can POST the access_token from the hash along with the new password.
- Footer: `<Link to="/login">Back to sign in</Link>`
- API call: `api('/auth/update-password', { method: 'POST', body: { password } })`

**7. Update `frontend/src/App.tsx`:**
Wire all routes:

```tsx
import { BrowserRouter, Routes, Route } from "react-router-dom";
import { SignUp } from "./pages/SignUp";
import { Login } from "./pages/Login";
import { VerifyEmail } from "./pages/VerifyEmail";
import { EmailConfirmed } from "./pages/EmailConfirmed";
import { ResetPassword } from "./pages/ResetPassword";
import { ResetPasswordConfirm } from "./pages/ResetPasswordConfirm";

function App() {
  return (
    <BrowserRouter>
      <div className="dark min-h-screen bg-background font-sans text-foreground antialiased">
        <Routes>
          <Route path="/signup" element={<SignUp />} />
          <Route path="/login" element={<Login />} />
          <Route path="/verify-email" element={<VerifyEmail />} />
          <Route path="/email-confirmed" element={<EmailConfirmed />} />
          <Route path="/reset-password" element={<ResetPassword />} />
          <Route path="/reset-password/confirm" element={<ResetPasswordConfirm />} />
          <Route path="/" element={<div className="flex items-center justify-center min-h-screen"><p className="text-muted-foreground">LLM Protein Designer — Logged in</p></div>} />
        </Routes>
      </div>
    </BrowserRouter>
  );
}

export default App;
```

**Styling rules from UI-SPEC (apply to all pages):**
- All form fields: gap lg (24px) between fields = `space-y-6` on the form container
- Label-to-input gap: sm (8px) = default shadcn FormItem spacing
- Submit buttons: full width (`w-full`), min height 44px (`min-h-[44px]`), primary variant
- Error text: destructive color, rendered by FormMessage component
- Loading spinner: Loader2 from lucide-react at 16px with `animate-spin`, placed left of text
- Footer links: use `text-sm text-muted-foreground` with Link components. "Sign in" / "Create one" / "Reset it" as the link text with `hover:text-foreground hover:underline`
- Input placeholder opacity: add `placeholder:text-muted-foreground/40` class to all Input components (40% opacity per UI-SPEC)
  </action>
  <verify>
    <automated>cd C:/Users/lab/Documents/Claude_projects/llm-proteinDesigner/frontend && test -f src/pages/SignUp.tsx && grep -q "Create your account" src/pages/SignUp.tsx && test -f src/pages/Login.tsx && grep -q "Sign in" src/pages/Login.tsx && test -f src/pages/VerifyEmail.tsx && grep -q "Check your inbox" src/pages/VerifyEmail.tsx && test -f src/pages/EmailConfirmed.tsx && grep -q "Email verified" src/pages/EmailConfirmed.tsx && test -f src/pages/ResetPassword.tsx && grep -q "Reset your password" src/pages/ResetPassword.tsx && test -f src/pages/ResetPasswordConfirm.tsx && grep -q "Set a new password" src/pages/ResetPasswordConfirm.tsx && grep -q "/signup" src/App.tsx && grep -q "/login" src/App.tsx && grep -q "/verify-email" src/App.tsx && grep -q "/email-confirmed" src/App.tsx && grep -q "/reset-password" src/App.tsx && npx tsc --noEmit 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/SignUp.tsx contains title "Create your account" and subtitle "Enter your email and a password to get started."
    - SignUp.tsx calls `api('/auth/signup', { method: 'POST'`
    - SignUp.tsx has zod validation for email, password (min 8), confirmPassword match
    - SignUp.tsx shows Loader2 spinner during submission
    - frontend/src/pages/Login.tsx contains title "Sign in" and subtitle "Welcome back."
    - Login.tsx calls `api('/auth/login', { method: 'POST'`
    - Login.tsx shows error "Incorrect email or password." on 401
    - frontend/src/pages/VerifyEmail.tsx contains title "Check your inbox"
    - VerifyEmail.tsx reads email from URL search params
    - frontend/src/pages/EmailConfirmed.tsx contains title "Email verified"
    - frontend/src/pages/ResetPassword.tsx contains title "Reset your password"
    - ResetPassword.tsx calls `api('/auth/reset-password', { method: 'POST'`
    - frontend/src/pages/ResetPasswordConfirm.tsx contains title "Set a new password"
    - ResetPasswordConfirm.tsx calls `api('/auth/update-password', { method: 'POST'`
    - frontend/src/App.tsx contains Route elements for all 6 paths: /signup, /login, /verify-email, /email-confirmed, /reset-password, /reset-password/confirm
    - `npx tsc --noEmit` exits without type errors
  </acceptance_criteria>
  <done>All 6 auth screens built with correct copy, form validation, loading states, error handling, and navigation per the UI-SPEC. Routes wired in App.tsx.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete auth flow end-to-end</name>
  <what-built>Complete authentication flow: 6 auth screens connected to FastAPI backend proxying Supabase Auth with HTTP-only cookies. Signup with email verification, login, session persistence, password reset.</what-built>
  <how-to-verify>
Prerequisites: Run `make dev` to start the full stack. Wait for all services to be healthy.

1. **Signup flow (AUTH-01 + AUTH-02):**
   - Visit http://localhost:5173/signup
   - Verify: Dark background, centered card, "Create your account" title, indigo submit button
   - Enter email: newuser@test.com, password: TestPass123!, confirm: TestPass123!
   - Click "Create account" -- verify spinner appears
   - Should redirect to /verify-email showing "Check your inbox" with the email address
   - Open http://localhost:54324 (Inbucket) -- verify email arrived with verification link
   - Click the verification link -- should redirect to /email-confirmed showing "Email verified"

2. **Login flow (AUTH-04):**
   - From /email-confirmed, click "Sign in"
   - On /login, enter newuser@test.com / TestPass123!
   - Click "Sign in" -- should redirect to / showing "Logged in"
   - Refresh the page -- should stay on / (session persists via cookie, AUTH-04)

3. **Login with test user:**
   - Visit http://localhost:5173/login
   - Enter test@example.com / Password123! (pre-verified seed user)
   - Should log in successfully

4. **Password reset (AUTH-03):**
   - Visit http://localhost:5173/reset-password
   - Enter test@example.com
   - Click "Send reset link"
   - Check Inbucket (http://localhost:54324) for reset email
   - Click reset link -- should open /reset-password/confirm
   - Enter new password, confirm, submit

5. **Validation errors:**
   - On /signup, submit empty form -- field errors appear
   - On /signup, enter mismatched passwords -- "Passwords do not match." appears
   - On /login, enter wrong password -- "Incorrect email or password." appears

6. **Visual checks:**
   - All screens use dark background (zinc-950)
   - Cards are zinc-900 with proper borders
   - Submit buttons are indigo-500
   - Focus rings are indigo on tab navigation
   - Footer links are muted, hover to white with underline
  </how-to-verify>
  <resume-signal>Type "approved" if the full auth flow works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All 6 routes render correct screens with correct copy per UI-SPEC
- Signup creates a user and redirects to verification pending
- Email arrives in Inbucket with verification link
- Login sets HTTP-only cookies and redirects to home
- Session persists across page refresh (AUTH-04)
- Password reset sends email and allows password change (AUTH-03)
- Form validation shows correct error messages
- Loading spinners appear during API calls
- Dark theme with indigo accent applied consistently
</verification>

<success_criteria>
Complete Phase 1 auth flow works end-to-end: signup -> email verification -> login -> session persistence -> password reset. All 6 screens match the UI-SPEC copywriting and visual contract. Human verification confirms the flow works correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
