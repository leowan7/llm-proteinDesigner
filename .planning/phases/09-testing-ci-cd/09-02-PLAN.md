---
phase: 09-testing-ci-cd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/vite.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/lib/api.test.ts
  - frontend/src/lib/utils.test.ts
  - frontend/src/lib/format.test.ts
  - frontend/src/pages/Login.test.tsx
  - frontend/src/pages/SettingsPage.test.tsx
  - frontend/src/pages/JobPage.test.tsx
  - frontend/src/pages/ChatPage.test.tsx
  - frontend/package.json
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "Vitest runs frontend unit tests with jsdom environment"
    - "API client functions have test coverage for fetch calls and error handling"
    - "Key page components render without crashing in smoke tests"
  artifacts:
    - path: "frontend/src/test/setup.ts"
      provides: "Vitest setup file with jest-dom matchers"
      min_lines: 5
    - path: "frontend/src/lib/utils.test.ts"
      provides: "Tests for utility functions"
      min_lines: 15
    - path: "frontend/src/pages/Login.test.tsx"
      provides: "Smoke test for Login page"
      min_lines: 15
    - path: "frontend/src/pages/SettingsPage.test.tsx"
      provides: "Smoke test for SettingsPage"
      min_lines: 15
  key_links:
    - from: "frontend/vite.config.ts"
      to: "frontend/src/test/setup.ts"
      via: "test.setupFiles array"
      pattern: "setupFiles.*setup"
    - from: "frontend/package.json"
      to: "vitest"
      via: "scripts.test"
      pattern: "\"test\".*vitest"
---

<objective>
Configure Vitest with jsdom and create frontend unit tests for API client functions, utility functions, and page component smoke tests per D-05 and D-07.

Purpose: The frontend has exactly one test file (api.test.ts). This plan establishes the Vitest testing infrastructure and adds smoke tests for key components and utility coverage.

Output: Vitest configured with setup file, `npm run test` script, tests for utils/format/api modules, and smoke render tests for 4 key pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-testing-ci-cd/09-CONTEXT.md
@.planning/phases/09-testing-ci-cd/09-RESEARCH.md

@frontend/vite.config.ts
@frontend/package.json
@frontend/src/lib/api.test.ts

<interfaces>
From frontend/vite.config.ts:
- test block exists with globals: true, environment: "jsdom", setupFiles: []
- @/ alias resolves to ./src

From frontend/package.json:
- devDependencies: vitest ^4.1.0, @testing-library/react ^16.3.2, @testing-library/jest-dom ^6.9.1, jsdom ^29.0.0
- No "test" script currently defined

From frontend/src/lib/utils.ts:
- export function cn(...inputs: ClassValue[]): string — clsx + tailwind-merge utility

From frontend/src/lib/format.ts:
- Utility functions for formatting dates, currency, durations (read file to get exact exports)

From frontend/src/lib/api.ts:
- Core API client: fetchWithAuth, login, signup, logout, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vitest infrastructure and write utility/API tests</name>
  <files>
    frontend/vite.config.ts,
    frontend/src/test/setup.ts,
    frontend/package.json,
    frontend/src/lib/api.test.ts,
    frontend/src/lib/utils.test.ts,
    frontend/src/lib/format.test.ts
  </files>
  <read_first>
    frontend/vite.config.ts,
    frontend/package.json,
    frontend/src/lib/api.test.ts,
    frontend/src/lib/api.ts,
    frontend/src/lib/utils.ts,
    frontend/src/lib/format.ts
  </read_first>
  <action>
1. Create `frontend/src/test/setup.ts`:
   ```typescript
   import "@testing-library/jest-dom/vitest";
   ```

2. Update `frontend/vite.config.ts` test block:
   - Change `setupFiles: []` to `setupFiles: ["./src/test/setup.ts"]`
   - Keep globals: true, environment: "jsdom" as-is

3. Add npm scripts to `frontend/package.json` scripts section:
   - `"test": "vitest run"`
   - `"test:watch": "vitest"`
   - `"test:coverage": "vitest run --coverage"`

4. Update `frontend/src/lib/api.test.ts`:
   - Read the existing file first. Extend it if tests already exist, or rewrite if minimal.
   - Test the core `fetchWithAuth` function by mocking `globalThis.fetch`:
     - `test_fetchWithAuth_success` — mock fetch returns 200 JSON, assert parsed result
     - `test_fetchWithAuth_401_redirects` — mock fetch returns 401, assert window.location changes to /login
     - `test_fetchWithAuth_network_error` — mock fetch throws, assert error propagated
   - Test `login(email, password)`:
     - `test_login_success` — mock fetch returns 200, assert returns user data
     - `test_login_invalid_credentials` — mock fetch returns 401, assert throws with message

5. Create `frontend/src/lib/utils.test.ts`:
   - Test `cn()` function:
     - `test_cn_merges_classes` — cn("px-2", "px-4") returns "px-4" (tailwind-merge deduplication)
     - `test_cn_handles_conditionals` — cn("base", false && "hidden", "text-sm") returns "base text-sm"
     - `test_cn_empty_args` — cn() returns ""

6. Create `frontend/src/lib/format.test.ts`:
   - Read `frontend/src/lib/format.ts` to discover exact export names and signatures.
   - Write tests for each exported function with representative inputs/outputs.
   - At minimum test: date formatting produces expected string format, currency formatting includes $ and correct decimal places, duration formatting handles seconds/minutes/hours.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/test/setup.ts contains "@testing-library/jest-dom/vitest"
    - frontend/vite.config.ts contains "setupFiles" referencing "setup.ts"
    - frontend/package.json scripts contains "test": "vitest run"
    - frontend/src/lib/utils.test.ts contains "test" or "it" with "cn"
    - frontend/src/lib/format.test.ts exists and contains at least 3 test cases
    - npx vitest run exits with code 0
  </acceptance_criteria>
  <done>Vitest configured with setup file and npm script. Utility and API client tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create page component smoke tests</name>
  <files>
    frontend/src/pages/Login.test.tsx,
    frontend/src/pages/SettingsPage.test.tsx,
    frontend/src/pages/JobPage.test.tsx,
    frontend/src/pages/ChatPage.test.tsx
  </files>
  <read_first>
    frontend/src/pages/Login.tsx,
    frontend/src/pages/SettingsPage.tsx,
    frontend/src/pages/JobPage.tsx,
    frontend/src/pages/ChatPage.tsx
  </read_first>
  <action>
1. For each page component, create a smoke test that verifies the component renders without crashing. Follow the pattern from 09-RESEARCH.md Pattern 3 (Vitest Component Smoke Test).

2. **Login.test.tsx**:
   - Render `<Login />` inside `<MemoryRouter>`.
   - Assert that the email input field is present: `screen.getByLabelText(/email/i)` or `screen.getByPlaceholderText(/email/i)`.
   - Assert that a submit button is present: `screen.getByRole("button", { name: /log in|sign in/i })`.

3. **SettingsPage.test.tsx**:
   - Mock `fetchWithAuth` from `@/lib/api` to return resolved data (avoid real API calls).
   - Render `<SettingsPage />` inside `<MemoryRouter>`.
   - Assert a heading or tab element is present (read the component first to determine the exact text).

4. **JobPage.test.tsx**:
   - Mock `fetchWithAuth` and `useParams` from react-router-dom to return `{ jobId: "test-job-123" }`.
   - Render `<JobPage />` inside `<MemoryRouter initialEntries={["/jobs/test-job-123"]}>` with a route wrapping it.
   - Assert the component renders without throwing (use `expect(container).toBeTruthy()`).

5. **ChatPage.test.tsx** (read ChatPage.tsx first — it may import heavy dependencies):
   - Mock SSE/agent dependencies if ChatPage imports them at the top level.
   - Render inside `<MemoryRouter>`.
   - Assert the chat input area is present.

NOTE: These are SMOKE tests only — assert structure (elements present), NOT computed styles or CSS values. jsdom does not process Tailwind CSS. If a component requires context providers (auth context, etc.), wrap with a minimal mock provider.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/Login.test.tsx contains "render" and "Login" and "getBy" or "queryBy"
    - frontend/src/pages/SettingsPage.test.tsx contains "render" and "SettingsPage"
    - frontend/src/pages/JobPage.test.tsx contains "render" and "JobPage"
    - frontend/src/pages/ChatPage.test.tsx contains "render" and "ChatPage"
    - npx vitest run exits with code 0 (all tests pass including page smoke tests)
  </acceptance_criteria>
  <done>4 page smoke tests pass. All components render without crashing in jsdom.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks -> real API | Tests mock fetch to avoid hitting real backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | test fixtures | mitigate | No real API URLs, tokens, or credentials in test files; use "http://test" base URL |
</threat_model>

<verification>
- `cd frontend && npx vitest run` exits 0
- At least 10 test cases pass across lib and pages directories
</verification>

<success_criteria>
- Vitest configured with jsdom, setup file, and npm test script
- API client, utils, and format modules have unit tests
- 4 key pages have smoke render tests
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-ci-cd/09-02-SUMMARY.md`
</output>
