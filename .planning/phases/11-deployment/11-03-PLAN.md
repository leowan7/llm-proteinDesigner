---
phase: 11
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - railway.toml
  - .github/workflows/deploy-modal.yml
  - .github/workflows/test.yml
files_touched_externally:
  - Cloudflare DNS zone (bindwave.com)
  - Railway (create backend-prod, worker-prod, backend-staging, worker-staging services)
  - Vercel (create kendrew project; add custom domains)
  - GitHub repo secrets (MODAL_TOKEN_ID, MODAL_TOKEN_SECRET, SMOKE_TEST_EMAIL, SMOKE_TEST_PASSWORD)
autonomous: false
requirements: []
user_setup:
  - service: cloudflare-dns
    why: "DNS records for bindwave.com (D-01, D-02)"
    env_vars: []
    dashboard_config:
      - task: "Add apex A record, www CNAME, app CNAME, app-staging CNAME, staging CNAME, CAA, DMARC/SPF/DKIM from 11-02"
        location: "dash.cloudflare.com → bindwave.com → DNS"
  - service: railway
    why: "Backend + worker containers (D-03, SC 2)"
    env_vars: []
    dashboard_config:
      - task: "Create 4 services: kendrew-backend-prod, kendrew-worker-prod, kendrew-backend-staging, kendrew-worker-staging"
        location: "railway.app → New Project → Deploy from GitHub repo"
      - task: "Paste Railway Variables from PROVISIONING.md into each service"
        location: "Railway service → Variables tab"
      - task: "Add custom domains: app.bindwave.com → backend-prod, app-staging.bindwave.com → backend-staging"
        location: "Railway service → Settings → Networking → Custom Domain"
  - service: vercel
    why: "Frontend hosting + custom domain + SSL (D-01, D-04, SC 1)"
    env_vars: []
    dashboard_config:
      - task: "Create kendrew Vercel project rooted at frontend/"
        location: "vercel.com → Add New → Project → import GitHub repo"
      - task: "Add Production domain bindwave.com + www redirect"
        location: "Project → Settings → Domains"
      - task: "Add Production domain staging.bindwave.com bound to 'staging' branch (via Git → Production branch override)"
        location: "Project → Settings → Git → Production Branch (configure staging branch alias)"
      - task: "Add env vars per environment: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_STRIPE_PUBLISHABLE_KEY, VITE_SENTRY_DSN_FRONTEND, VITE_APP_BASE_URL"
        location: "Project → Settings → Environment Variables (Production + Preview scoped)"

tags: [deployment, railway, vercel, modal, dns, cicd]

must_haves:
  truths:
    - "bindwave.com serves the Vercel frontend over HTTPS with a valid cert (SC 1)"
    - "app.bindwave.com serves the Railway backend over HTTPS with a valid LetsEncrypt cert (SC 2)"
    - "Pushing to main auto-deploys Railway backend + worker Docker containers (SC 2)"
    - "Supabase db push runs as Railway preDeployCommand before traffic shifts; failure aborts deploy (SC 3, D-06)"
    - "REDIS_URL in Railway Variables uses rediss:// scheme (SC 4)"
    - "deploy-modal.yml has no `if: false` guard and pins modal>=1.4,<2; pushes to main deploy Modal `main` env; PRs touching infrastructure/modal/** deploy to `staging` (SC 6, D-07)"
    - "All DNS records (apex A, www CNAME, app CNAME grey, app-staging CNAME grey, staging CNAME orange, DMARC/SPF/DKIM TXT, CAA) are live in Cloudflare"
    - "No Supabase service_role key or Stripe secret leaks into Vercel Environment Variables (SC 7, D-12)"
  artifacts:
    - path: "railway.toml"
      provides: "Railway build + predeploy + start config (Docker, supabase db push, uvicorn)"
      contains: "preDeployCommand"
    - path: ".github/workflows/deploy-modal.yml"
      provides: "Enabled Modal deploy workflow (no if:false, Modal 1.4.x)"
    - path: ".github/workflows/test.yml"
      provides: "Frontend bundle secret-leak grep — lands in Wave 2 (before Wave 2 Vercel deploys) per revision fix Blocker 3 Option A"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
  key_links:
    - from: "railway.toml"
      to: "backend/Dockerfile supabase CLI"
      via: "preDeployCommand: supabase db push"
      pattern: "preDeployCommand"
    - from: "Cloudflare DNS app.bindwave.com grey"
      to: "Railway LetsEncrypt cert issuance"
      via: "DNS-only CNAME to Railway target"
      pattern: "app.bindwave.com"
    - from: "deploy-modal.yml"
      to: "Modal main + staging envs"
      via: "modal deploy --env $MODAL_ENVIRONMENT"
      pattern: "modal deploy"
---

> **Session marker 2026-05-26** — backend prod LIVE (`https://app.bindwave.com/health` → 200) + master branch protection now ACTIVE (PR + 1-review required, no force-push, no deletions, conversation resolution required; admin bypass enabled for solo-dev workflow; no status-check requirements until CI is cleaned up). PR #1 closed without merging — pre-existing CI debt (~30 frontend TS errors + 2 backend auth-test flakes) made the gate-with-checks plan unworkable in this session. Two side-fixes landed master-direct: `2f8ed1a` (ruff E402 hoist) + `1a758f3` (vitest e2e exclude). See `~/vault/Claude Vault/projects/kendrew/phase11-handoff.md` → "2026-05-26 (autonomous drive — continued)" for full pivot + browser-only items still pending.

<objective>
Wave 2 of Phase 11: cut DNS over, stand up Railway + Vercel platforms with env vars + custom domains, and flip on the Modal deploy workflow.

Purpose: Wave 1 produced secrets; Wave 2 turns them into running services reachable at `bindwave.com` and `app.bindwave.com`. Per D-05 every future push to main will auto-deploy once CI passes.

Output: railway.toml committed, deploy-modal.yml enabled and pinned to Modal 1.4.x, Cloudflare DNS records live, Railway backend + worker + Vercel frontend deployed for both prod and staging. Also: CI frontend-bundle secret-leak grep added to test.yml (moved here from Plan 11-05 per revision — must land before Wave 2's first Vercel deploy to guard SC 7).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-deployment/11-CONTEXT.md
@.planning/phases/11-deployment/11-RESEARCH.md
@.planning/phases/11-deployment/11-02-PROVISIONING.md
@.github/workflows/deploy-modal.yml
@.github/workflows/test.yml
@backend/Dockerfile
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create railway.toml, flip on deploy-modal.yml, and add frontend-bundle secret-leak grep to test.yml</name>
  <files>railway.toml, .github/workflows/deploy-modal.yml, .github/workflows/test.yml</files>
  <read_first>
    - .github/workflows/deploy-modal.yml (current state: `if: ${{ false }}` workflow-disable guard near top of the `deploy` job; `pip install 'modal>=0.63,<1'` in the install step; secondary `if: ${{ false && github.event_name == 'pull_request' }}` guard on the PR-to-staging step)
    - backend/Dockerfile (confirms Plan 11-01 added supabase CLI — required for predeploy)
    - backend/requirements.txt (confirms modal==1.4.2 pin — deploy-modal.yml must match)
    - .github/workflows/test.yml (frontend-unit job at line 59 — add build+grep step after `npm run test`)
    - .planning/phases/11-deployment/11-RESEARCH.md §Pattern 1 (railway.toml), §Pitfall 5 (service-role must not reach frontend), §Pitfall 7 (Modal version), §Code Example 1
  </read_first>
  <action>
**[BLOCKING]** Three changes — create railway.toml at repo root, modify deploy-modal.yml, and add a frontend-bundle secret-leak grep to test.yml.

**Create `railway.toml` at repo root** with this exact content:

```toml
# Phase 11 D-06: Railway config-as-code.
# preDeployCommand runs Supabase SQL migrations before traffic shifts.
# Dockerfile bundles Supabase CLI (Plan 11-01 Task 1).
# Failed migration aborts the rollout.

[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
preDeployCommand = ["supabase", "db", "push", "--db-url", "$DATABASE_URL", "--yes"]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT --proxy-headers --forwarded-allow-ips='*'"
healthcheckPath = "/health"
healthcheckTimeout = 30
numReplicas = 2
restartPolicyType = "ON_FAILURE"
```

Leo notes: Railway UI lets you override settings per service, but `railway.toml` is the source of truth for backend-prod. Worker services (kendrew-worker-prod, kendrew-worker-staging) should be created in Railway UI with `startCommand = "arq worker.main.WorkerSettings"` and NO preDeployCommand (worker doesn't run migrations). Staging backend service should override `preDeployCommand` in Railway UI only if staging uses a different DATABASE_URL — but with per-env Railway Variables, the same `railway.toml` works.

**Modify `.github/workflows/deploy-modal.yml`** with three pattern-based edits (do NOT use absolute line numbers — line numbers drift; match by pattern):

1. **Top-level workflow-disable guard:** Locate the line matching `if: \${{ false }}` (a single static-false guard used to disable the workflow as a whole — the "draft" gate). Replace that entire line with a comment noting the removal: `    # Guard removed 2026-04-24 (Phase 11 D-07). Workflow is now live.`

2. **Modal pin:** Locate the line matching `modal>=0\.63,<1` (inside a `pip install ...` command). Replace the version spec so the line reads `pip install 'modal>=1.4,<2'` (preserve surrounding quoting style).

3. **PR-to-staging guard:** Locate the line matching `if: \${{ false && github.event_name == 'pull_request'` (a guard that AND-s static false with the PR event check — intended to be enabled per the draft's documented intent). Replace with `    if: ${{ github.event_name == 'pull_request' }}` so PR events to Modal paths deploy to the staging env.

Any other `if: false` or `if: \${{ false ...` guards that remain in the file after these edits: enable them per the draft's documented intent (each such guard has an adjacent comment describing what it was meant to gate). After the edits, `grep -E "if:\s*(false|\\\$\\{\\{\\s*false)" .github/workflows/deploy-modal.yml` MUST return zero matches.

Do NOT rename the workflow file. Prefer: change the workflow's `name:` key from `Deploy Modal apps (DRAFT — not enabled)` (or similar "DRAFT" variant) to `Deploy Modal apps` so the GitHub UI does not mislabel runs. After edits:
- `grep -E "if:\s*(false|\\\$\\{\\{\\s*false)" .github/workflows/deploy-modal.yml` MUST return zero matches
- `grep "modal>=1.4,<2" .github/workflows/deploy-modal.yml` MUST match
- `grep -i DRAFT .github/workflows/deploy-modal.yml` MUST return zero matches in the `name:` line

**Modify `.github/workflows/test.yml`** to add a frontend-bundle secret-leak grep (moved from Plan 11-05 Task 1 per revision fix Blocker 3 Option A; it MUST land in Wave 2 before Wave 2 Task 2 Block C deploys to Vercel).

Locate the `frontend-unit` job (jobs.frontend-unit, starting at line 59). After the existing `Run Vitest` step (line 81-82), append two new steps as part of the same job:

```yaml
      - name: Build frontend bundle (prerequisite for secret-leak grep)
        run: npm run build

      - name: "Guard: no server secrets in frontend bundle (Phase 11 SC 7 + Pitfall 5)"
        run: |
          if grep -rE "SUPABASE_SERVICE_ROLE_KEY|STRIPE_SECRET_KEY|ANTHROPIC_API_KEY|MODAL_TOKEN_SECRET|WEBHOOK_HMAC_SECRET" dist/ ; then
            echo "::error::Server-side secret leaked into frontend bundle. Fix before merge."
            exit 1
          fi
          echo "PASS: no server secrets found in frontend/dist/"
```

These steps run inside the `frontend-unit` job which already has `working-directory: frontend` as its default — so `npm run build` and `dist/` resolve relative to `frontend/`. Do NOT add this step elsewhere; keep it in one place so it has a single owner. Do NOT change any existing gate.
  </action>
  <acceptance_criteria>
- `railway.toml` exists at repo root
- `railway.toml` contains literal `preDeployCommand` (grep: `grep preDeployCommand railway.toml`)
- `railway.toml` contains literal `supabase` and `db` and `push` in the preDeployCommand array
- `railway.toml` contains `healthcheckPath = "/health"`
- `railway.toml` contains `startCommand = "uvicorn main:app`
- `grep -cE "if:\s*(false|\\\$\\{\\{\\s*false)" .github/workflows/deploy-modal.yml` returns 0 (no boolean-false guards remain)
- `grep "modal>=1.4,<2" .github/workflows/deploy-modal.yml` matches
- `grep "modal>=0.63,<1" .github/workflows/deploy-modal.yml` returns 0 matches (old pin removed)
- `grep -i DRAFT .github/workflows/deploy-modal.yml` returns 0 matches in the `name:` line
- YAML lints clean: `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-modal.yml'))"` exits 0
- `grep -q "SUPABASE_SERVICE_ROLE_KEY" .github/workflows/test.yml` (frontend-bundle grep landed here in Wave 2)
- `grep -q "STRIPE_SECRET_KEY" .github/workflows/test.yml`
- `grep -q "WEBHOOK_HMAC_SECRET" .github/workflows/test.yml`
- `grep -q "npm run build" .github/workflows/test.yml` (prerequisite step present)
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f railway.toml && grep -q "preDeployCommand" railway.toml && grep -q "supabase" railway.toml && grep -q "modal>=1.4,<2" .github/workflows/deploy-modal.yml && ! grep -qE "if:\s*(false|\\\$\\{\\{\\s*false)" .github/workflows/deploy-modal.yml && grep -q "SUPABASE_SERVICE_ROLE_KEY" .github/workflows/test.yml && python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-modal.yml')); yaml.safe_load(open('.github/workflows/test.yml'))"</automated>
  </verify>
  <done>railway.toml committed with Supabase predeploy + /health check; deploy-modal.yml live with Modal 1.4.x pin and no `if: false` guard; test.yml blocks merges that leak server secrets into the Vercel bundle before Wave 2 deploys.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Cloudflare DNS + Railway services + Vercel project + GitHub secrets</name>
  <files>(external dashboards only — no repo files modified)</files>
  <read_first>
    - .planning/phases/11-deployment/11-02-PROVISIONING.md (all secrets collected in Plan 11-02)
    - .planning/phases/11-deployment/11-RESEARCH.md §Pattern 3 (DNS record table with exact values)
    - .planning/phases/11-deployment/11-RESEARCH.md §Pitfall 3 (Cloudflare proxy vs LetsEncrypt gotcha)
    - .planning/phases/11-deployment/11-RESEARCH.md §Pitfall 5 (service-role key must NOT go to Vercel)
    - .planning/phases/11-deployment/11-RESEARCH.md §Pitfall 8 (use GitHub branch protection, not Vercel Deployment Checks)
    - .planning/phases/11-deployment/11-CONTEXT.md §D-10 (canonical webhook secret name is WEBHOOK_HMAC_SECRET + _PREV; RUNPOD_WEBHOOK_SECRET is a deprecated backend-side alias only — NOT the Railway-deployed variable name)
    - .planning/phases/11-deployment/11-CONTEXT.md §D-11 and D-12 (env-scope matrix)
    - backend/config.py (Settings field names — Railway Variables MUST match UPPER_SNAKE_CASE exactly)
  </read_first>
  <action>
Human-executed platform wiring. Claude has already committed railway.toml, the enabled deploy-modal.yml, and the frontend-bundle secret-leak grep in test.yml (Task 1). Leo now clicks through 4 external dashboards in order: (A) Cloudflare DNS — add apex A 76.76.21.21 orange, www CNAME orange, app/app-staging CNAMEs grey, staging CNAME orange, CAA, DMARC/SPF/DKIM TXT, MX grey; (B) Railway — create 4 services (backend-prod, worker-prod, backend-staging, worker-staging), paste env vars from PROVISIONING.md per the exact backend/config.py field names (using `WEBHOOK_HMAC_SECRET` + `WEBHOOK_HMAC_SECRET_PREV` — the canonical D-10 names — NOT `RUNPOD_WEBHOOK_SECRET`), add custom domains app.bindwave.com + app-staging.bindwave.com; (C) Vercel — create kendrew project rooted at frontend/, add ONLY the 5 whitelisted VITE_ env vars, add bindwave.com + staging.bindwave.com domains; (D) GitHub repo secrets — MODAL_TOKEN_ID, MODAL_TOKEN_SECRET, SMOKE_TEST_EMAIL, SMOKE_TEST_PASSWORD + enable branch protection requiring test.yml gates (NOT Vercel Deployment Checks, per Pitfall 8). See <how-to-verify> for block-by-block steps.
  </action>
  <what-built>
Claude committed railway.toml, the enabled deploy-modal.yml, and the CI frontend-bundle secret-leak grep in test.yml (the last one blocks Wave 2 Vercel deploys that would bake a server secret into the bundle). Leo now clicks through Cloudflare, Railway, Vercel, and GitHub Settings to wire the platforms. None of this is scriptable at the dashboard level.

Order: DNS first (LetsEncrypt needs it), then Railway (has DB dep), then Vercel (no deps), then GitHub secrets (deploy-modal.yml needs them).
  </what-built>
  <how-to-verify>
**Block A — Cloudflare DNS records** (dash.cloudflare.com → bindwave.com → DNS):

Add these exact records. Follow RESEARCH Pattern 3 table literally. MX and `_dkim` records must be DNS-only (grey cloud) per Pitfall 6. `app.bindwave.com` must be DNS-only per Pitfall 3 (proxy breaks LetsEncrypt).

| Name | Type | Value | Proxy | TTL |
|------|------|-------|-------|-----|
| `bindwave.com` (`@`) | A | `76.76.21.21` | DNS only (grey) | Auto |
| `www` | CNAME | `cname.vercel-dns.com` | DNS only (grey) | Auto |
| `app` | CNAME | `<railway-backend-prod target>.up.railway.app` | **DNS only (grey)** | Auto |
| `app-staging` | CNAME | `<railway-backend-staging target>.up.railway.app` | **DNS only (grey)** | Auto |
| `staging` | CNAME | `cname.vercel-dns.com` | DNS only (grey) | Auto |
| `@` | CAA | `0 issue "letsencrypt.org"` | — | Auto |
| `@` | CAA | `0 issue "pki.goog"` | — | Auto |
| `_dmarc` | TXT | `v=DMARC1; p=none; rua=mailto:dmarc@bindwave.com` | — | Auto |
| (Resend-specified) | TXT (SPF) | (Resend-provided) | — | Auto |
| `resend._domainkey` | TXT (DKIM) | (Resend-provided) | — | Auto |
| (Resend-specified) | MX | (Resend-provided) | **DNS only (grey)** | Auto |

After adding, `dig +short A bindwave.com` returns `76.76.21.21` and `dig +short CNAME app.bindwave.com` returns a `*.up.railway.app` target.

**Block B — Railway services** (railway.app → New Project → Deploy from GitHub Repo):

1. Connect to the `llm-proteinDesigner` GitHub repo. Select branch: `main`.
2. Create first service: `kendrew-backend-prod`.
   - Railway auto-detects `railway.toml` at repo root. Confirm "Builder: Dockerfile" at `backend/Dockerfile`.
   - Settings → Variables → paste Backend env vars from PROVISIONING.md. Full list (use these exact names — match backend/config.py):
     - `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_JWT_SECRET`
     - `DATABASE_URL` (the Supavisor transaction pooler URL — port 6543)
     - `S3_ENDPOINT_URL`, `S3_ACCESS_KEY`, `S3_SECRET_KEY`, `S3_BUCKET_NAME=kendrew-prod`
     - `REDIS_URL` (rediss:// scheme — TLS)
     - `CSRF_SECRET` (generate: `openssl rand -hex 32`)
     - `COOKIE_SECURE=true`
     - `CORS_ORIGINS=["https://bindwave.com"]`
     - `DEBUG=false`, `TESTING=false`
     - `ANTHROPIC_API_KEY`
     - `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`
     - `GPU_PROVIDER=modal`
     - `MODAL_TOKEN_ID`, `MODAL_TOKEN_SECRET`, `MODAL_WORKSPACE`, `MODAL_ENVIRONMENT=main`
     - `RESEND_API_KEY`, `RESEND_FROM_EMAIL=Kendrew.AI <jobs@bindwave.com>`
     - `APP_BASE_URL=https://app.bindwave.com`
     - `SENTRY_DSN` (leave empty for now — Plan 11-05 sets this)
     - `WEBHOOK_HMAC_SECRET` = `openssl rand -hex 32` output  (D-10 canonical name — shared secret for Modal + RunPod webhook HMAC; rotated via _PREV pair per docs/deploy.md runbook)
     - `WEBHOOK_HMAC_SECRET_PREV` = (empty at first provision; populated only during rotation windows per D-10 runbook)
     - DO NOT set `RUNPOD_WEBHOOK_SECRET` in Railway. It remains as a deprecated `model_post_init` alias inside `backend/config.py` (Plan 11-04) for one release cycle, but Railway operationally only sets the new `WEBHOOK_HMAC_SECRET*` names — setting both would cause rotation drift (docs/deploy.md rotation runbook references the new name as source of truth).
   - Settings → Networking → Add Custom Domain → `app.bindwave.com`. Wait for Railway cert green (~5 min).
3. Create `kendrew-worker-prod` service. Same repo. Override settings:
   - Builder: Dockerfile at `backend/Dockerfile`
   - Start command: `arq worker.main.WorkerSettings`
   - NO healthcheck, NO preDeployCommand
   - Copy all the same Variables from backend-prod.
4. Create `kendrew-backend-staging` + `kendrew-worker-staging`. Same pattern. Variables point to staging resources:
   - `DATABASE_URL` → kendrew-staging Supabase
   - `REDIS_URL` → kendrew-staging Upstash
   - `S3_BUCKET_NAME=kendrew-staging`
   - `CORS_ORIGINS=["https://staging.bindwave.com"]`
   - `APP_BASE_URL=https://app-staging.bindwave.com`
   - `MODAL_ENVIRONMENT=staging`
   - `STRIPE_SECRET_KEY` = Stripe **test mode** key
   - `WEBHOOK_HMAC_SECRET` = separate `openssl rand -hex 32` value (do not reuse prod secret)
   - `WEBHOOK_HMAC_SECRET_PREV` = empty
   - Add custom domain `app-staging.bindwave.com` to backend-staging service
5. Trigger first deploys. Watch Railway logs — confirm `supabase db push` runs in predeploy phase without errors, then uvicorn starts, then `/health` returns 200.

**Block C — Vercel frontend** (vercel.com → Add New → Project):

1. Import the GitHub repo. **Root directory: `frontend`**. Framework preset: Vite.
2. Build command: `npm run build`. Output directory: `dist`.
3. Environment Variables → Add per-environment (Production + Preview). **ONLY these 5 keys** (SC 7 — Pitfall 5):
   - `VITE_SUPABASE_URL` (both envs — same Supabase per env match Railway's)
   - `VITE_SUPABASE_ANON_KEY` (**NEVER service_role** — Pitfall 5)
   - `VITE_STRIPE_PUBLISHABLE_KEY` (live key for prod, test key for preview/staging)
   - `VITE_SENTRY_DSN_FRONTEND` (leave empty for now; Plan 11-05 fills)
   - `VITE_APP_BASE_URL=https://app.bindwave.com` for prod, `https://app-staging.bindwave.com` for preview
4. Domains → Add `bindwave.com` (prod). Add `www.bindwave.com` with redirect → `bindwave.com`.
5. Domains → Add `staging.bindwave.com` (bind to a `staging` git branch; on push to that branch, Vercel deploys to this domain).
6. Deploy. Confirm `curl -sI https://bindwave.com` returns 200 with `strict-transport-security`.

**Block D — GitHub repo secrets** (github.com/<owner>/llm-proteinDesigner → Settings → Secrets and variables → Actions):

Add these repository secrets (they flow into Actions workflows):
- `MODAL_TOKEN_ID` (from PROVISIONING.md)
- `MODAL_TOKEN_SECRET` (from PROVISIONING.md)
- `SMOKE_TEST_EMAIL` (create a test account on prod: e.g., `smoke@bindwave.com`)
- `SMOKE_TEST_PASSWORD` (password for that account)

Enable GitHub branch protection on `main`:
- Settings → Branches → Add rule → Branch name pattern: `main`
- Require pull request reviews: (optional for solo dev)
- **Require status checks before merging** → Require branches up to date → add required checks from test.yml: `Backend Tests`, `Frontend Unit Tests`, `E2E Tests`, `Lint & Type Check`
- Do NOT enable Vercel Deployment Checks (Pitfall 8 — race condition)

After completing Block D, push a no-op commit to main and confirm: Railway + Vercel auto-deploy; deploy-modal.yml triggers with the matrix (or does not trigger if the no-op touched nothing in `infrastructure/modal/**`).
  </how-to-verify>
  <resume-signal>
After all 4 blocks complete, type "deployed" and paste:
- `curl -sI https://bindwave.com` first line (expect `HTTP/2 200`)
- `curl -sI https://app.bindwave.com/health` first line (expect `HTTP/2 200`)
- Railway service list (prod backend, prod worker, staging backend, staging worker — all running)
- `dig +short CNAME app.bindwave.com` output
- GitHub Actions run history showing deploy-modal.yml ran (or was skipped correctly)

If any block fails (e.g., Railway cert stuck on "Validating" — check Pitfall 3 grey cloud; Vercel build fails — check env vars), describe the blocker.
  </resume-signal>
  <acceptance_criteria>
- `curl -sI https://bindwave.com` returns HTTP 200 with `strict-transport-security` header (SC 1)
- `curl -sf https://app.bindwave.com/health` returns 200 (SC 2)
- `curl -sf https://app-staging.bindwave.com/health` returns 200 (staging equivalent)
- `dig +short A bindwave.com` returns `76.76.21.21`
- `dig +short CNAME app.bindwave.com` returns a target ending in `.up.railway.app`
- `dig +short CNAME staging.bindwave.com` returns `cname.vercel-dns.com`
- Railway backend-prod service logs show `supabase db push` completed before uvicorn started (confirm in Railway UI or `railway logs`)
- `curl -sf https://app.bindwave.com/health | jq .` returns structured JSON (keys may vary — Plan 11-04 adds full db/redis/r2 breakdown)
- GitHub Actions: last workflow_run for `deploy-modal.yml` does NOT contain `if: false` skip reason
- Vercel Environment Variables list (for `kendrew` project, Production scope) does NOT contain `SUPABASE_SERVICE_ROLE_KEY` or `STRIPE_SECRET_KEY` (SC 7)
- `grep -r "SUPABASE_SERVICE_ROLE\|STRIPE_SECRET" frontend/dist/` returns 0 matches after a Vercel build (SC 7) — also enforced in CI by Task 1's test.yml grep
- Railway backend-prod Variable `REDIS_URL` starts with `rediss://` (TLS — SC 4)
- Railway backend-prod Variable `DATABASE_URL` contains `pooler.supabase.com:6543` (Supavisor transaction mode — SC 3)
- Railway backend-prod Variable list contains `WEBHOOK_HMAC_SECRET` (canonical D-10 name)
- Railway backend-prod Variable list does NOT contain `RUNPOD_WEBHOOK_SECRET` (deprecated alias is backend-only, not Railway-deployed)
  </acceptance_criteria>
  <verify>
    <automated>curl -sf https://bindwave.com -o /dev/null -w "%{http_code}\n" | grep -q 200 && curl -sf https://app.bindwave.com/health -o /dev/null -w "%{http_code}\n" | grep -q 200 && dig +short CNAME app.bindwave.com | grep -q up.railway.app && dig +short A bindwave.com | grep -q 76.76.21.21</automated>
  </verify>
  <done>DNS live; Railway runs backend + worker for prod + staging with predeploy migrations and WEBHOOK_HMAC_SECRET* set (not RUNPOD_WEBHOOK_SECRET); Vercel serves bindwave.com + staging.bindwave.com; deploy-modal.yml is live; GitHub branch protection gates main on test.yml (which now includes the frontend-bundle secret-leak grep).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Cloudflare → Vercel | Public HTTPS (HSTS, platform cert) |
| Internet → Cloudflare DNS-only → Railway | Public HTTPS (LetsEncrypt platform cert) |
| GitHub Actions → Modal | MODAL_TOKEN_ID/SECRET repo secrets; fork PRs don't receive by default |
| Railway Variables → Docker container | Secrets injected as env; never logged |
| Vercel Env Vars → browser bundle | VITE_ prefix inlined at build — MUST NOT include server-side secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Information Disclosure | Supabase service_role key bundled into Vercel frontend (Pitfall 5) | mitigate | Block C step 3 enumerates ALLOWED VITE_ vars; acceptance grep `grep -r SUPABASE_SERVICE_ROLE frontend/dist/` must return 0; Task 1 CI grep in test.yml blocks merges that would leak (moved here from Plan 11-05 per revision fix) |
| T-11-03-02 | Spoofing | DNS hijack via Cloudflare API token compromise (CONTEXT seed) | mitigate | Token scoped to Zone:DNS:Edit for bindwave.com zone only (Plan 11-02 Block E); Cloudflare 2FA required |
| T-11-03-03 | Tampering | Cloudflare proxy breaks LetsEncrypt for app.bindwave.com (Pitfall 3) | mitigate | Block A table explicitly requires DNS-only (grey cloud) for `app` and `app-staging` |
| T-11-03-04 | Information Disclosure | Staging leaking prod user data (CONTEXT seed) | mitigate | Separate Supabase projects + R2 buckets + Upstash DBs (Plan 11-02); Railway Variables point to per-env resources; no db dump/restore cross-env |
| T-11-03-05 | Information Disclosure | CORS too permissive → cross-site credential leak | mitigate | `CORS_ORIGINS=["https://bindwave.com"]` exact match in prod; staging gets `["https://staging.bindwave.com"]` |
| T-11-03-06 | Spoofing | Stripe webhook receiving events for wrong env (CONTEXT seed) | mitigate | Prod uses live `STRIPE_SECRET_KEY`, staging uses test key; Stripe webhook URLs are env-specific URLs (`app.bindwave.com/webhooks/stripe` vs `app-staging.bindwave.com/webhooks/stripe`) |
| T-11-03-07 | Tampering | Vercel Deployment Checks + GitHub branch protection race (Pitfall 8) | mitigate | Block D explicitly disables Vercel Deployment Checks; use GitHub branch protection only |
| T-11-03-08 | Elevation of Privilege | DEBUG=true in prod → /debug/sentry-test reachable | mitigate | Block B enumerates `DEBUG=false` for prod Railway Variables; Plan 11-01 /debug/sentry-test returns 404 when debug=false |
| T-11-03-09 | Information Disclosure | R2 bucket left public (CONTEXT seed) | mitigate | R2 default-private asserted in Plan 11-02 acceptance; `S3_BUCKET_NAME` per-env prevents cross-write |
| T-11-03-10 | Tampering | Partial Supabase migration interrupted (CONTEXT seed) | mitigate | preDeployCommand runs ONCE per deploy and aborts on failure; Plan 11-05 rollback drill covers recovery; manual rollback path documented in docs/deploy.md (Plan 11-05) |
| T-11-03-11 | Tampering | Rotation drift — Railway has `RUNPOD_WEBHOOK_SECRET` set alongside `WEBHOOK_HMAC_SECRET*`, causing config.py post-init to silently override | mitigate | Block B explicitly instructs Leo to set only `WEBHOOK_HMAC_SECRET` + `WEBHOOK_HMAC_SECRET_PREV` in Railway; deprecated alias is config.py-only per D-10 |
</threat_model>

<verification>
- `curl -sI https://bindwave.com` returns 200 with HSTS header
- `curl -sI https://app.bindwave.com/health` returns 200
- `curl -sI https://staging.bindwave.com` returns 200
- `curl -sI https://app-staging.bindwave.com/health` returns 200
- `dig +short CNAME app.bindwave.com` returns Railway target
- GitHub Actions: deploy-modal.yml ran on a test PR touching infrastructure/modal/
- Vercel build artifacts contain no server-side secret keys
- CI test.yml frontend-unit job now runs `npm run build` + secret-leak grep
</verification>

<success_criteria>
Platform deploys are live and self-deploying from main. SC 1, SC 2, SC 3 (pooling), SC 4 (TLS), SC 5 (R2 reachable), SC 6 (Modal deploy workflow live), and SC 7 (platform-native secrets with correct scoping + CI bundle-grep guard) are met at the infrastructure level. Code-level wiring (dual-secret webhook, asyncpg fix, spend alert env, Sentry DSN) lands in Plan 11-04.
</success_criteria>

<output>
After completion, create `.planning/phases/11-deployment/11-03-SUMMARY.md`.

Summary file contains no emoji characters (verify: `python -c "import re,sys; sys.exit(bool(re.search(r'[\U0001F300-\U0001F9FF\u2600-\u27BF]', open('.planning/phases/11-deployment/11-03-SUMMARY.md').read())))"` exits 0).
</output>
