---
phase: 11
plan: 05
type: execute
wave: 4
depends_on: ["11-03", "11-04"]
files_modified:
  - backend/main.py
  - docs/deploy.md
  - .planning/ROADMAP.md
files_touched_externally:
  - Sentry (create kendrew-backend + kendrew-frontend projects, Slack integration)
  - UptimeRobot (create 2 monitors)
  - Railway (paste SENTRY_DSN into backend-prod + backend-staging Variables)
  - Vercel (paste VITE_SENTRY_DSN_FRONTEND into prod + preview envs)
autonomous: false
requirements: []
user_setup:
  - service: sentry
    why: "Error tracking + Performance transactions on hot paths (D-13, D-14, SC 8)"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry → kendrew-backend project → Settings → Client Keys (DSN)"
      - name: VITE_SENTRY_DSN_FRONTEND
        source: "Sentry → kendrew-frontend project → Settings → Client Keys (DSN)"
    dashboard_config:
      - task: "Create kendrew-backend Python project + kendrew-frontend Browser/React project"
        location: "sentry.io → Projects → Create Project"
      - task: "Configure Slack integration routing to #kendrew-alerts"
        location: "Sentry → Settings → Integrations → Slack"
  - service: uptimerobot
    why: "External liveness ping every 5 min (D-13, SC 8)"
    env_vars: []
    dashboard_config:
      - task: "Create HTTPS monitor: https://app.bindwave.com/health @ 5min, Slack + email contacts"
        location: "uptimerobot.com → Add Monitor"
      - task: "Create second monitor: https://app-staging.bindwave.com/health @ 5min"
        location: "uptimerobot.com → Add Monitor"
  - service: slack
    why: "Receive Sentry + UptimeRobot + deploy-smoke alerts (#kendrew-alerts)"
    env_vars: []
    dashboard_config:
      - task: "Create incoming webhook for #kendrew-alerts channel (if not already present from Phase 5)"
        location: "api.slack.com/apps → Incoming Webhooks"

tags: [deployment, sentry, uptime, monitoring, docs, roadmap-correction]

must_haves:
  truths:
    - "Sentry receives a synthetic 500 from /debug/sentry-test on staging and routes to #kendrew-alerts (SC 8)"
    - "Sentry Performance traces are captured for POST /agent/*, POST /jobs/launch, POST /webhooks/runpod, POST /webhooks/heartbeat, POST /jobs/{job_id}/upload-urls (D-14)"
    - "UptimeRobot monitors exist for app.bindwave.com/health and app-staging.bindwave.com/health at 5-min intervals (D-13, SC 8)"
    - "docs/deploy.md exists and documents the per-runtime env matrix (D-12) + secret rotation runbook (D-10) + 5-min rollback drill (SC 9)"
    - "ROADMAP.md Phase 11 SC 6 reads 'Modal (primary GPU provider per Phase 10), RunPod quarantined as emergency fallback only' (CONTEXT corrections)"
    - "ROADMAP.md Phase 11 SC 8 reads 'Monitoring: Sentry for errors + UptimeRobot for uptime, both routing to #kendrew-alerts Slack' (CONTEXT corrections)"
    - "smoke.yml runs against prod after deploy and /health returns 200 within 2s (SC 2)"
  artifacts:
    - path: "backend/main.py"
      provides: "Sentry init uses traces_sampler to sample 100% of hot paths (D-14)"
      contains: "traces_sampler"
    - path: "docs/deploy.md"
      provides: "Env matrix + rotation runbook + rollback drill + subprocessor references"
      contains: "WEBHOOK_HMAC_SECRET"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 SC 6 and SC 8 corrected to Modal + UptimeRobot/Sentry/Slack"
  key_links:
    - from: "backend/main.py Sentry traces_sampler"
      to: "5 hot paths per D-14"
      via: "transaction_context.name match set"
      pattern: "POST /agent"
    - from: "UptimeRobot monitor"
      to: "app.bindwave.com/health"
      via: "HTTPS GET every 5 min, alert to #kendrew-alerts"
      pattern: "/health"
    - from: "docs/deploy.md rotation runbook"
      to: "webhook_hmac_secret + webhook_hmac_secret_prev (Plan 11-04)"
      via: "5-step rotation procedure"
      pattern: "WEBHOOK_HMAC_SECRET_PREV"
---

<objective>
Wave 4 of Phase 11: wire monitoring (Sentry + UptimeRobot), write the deploy runbook, and correct ROADMAP.md SC 6 + SC 8 to match CONTEXT decisions.

Purpose: SC 8 (monitoring), SC 9 (rollback in 5 min) cannot be marked green without this work. ROADMAP corrections close the loop on CONTEXT's "ROADMAP Corrections Required" section so the document agrees with what was actually built.

Note: the frontend-bundle secret-leak CI grep (SC 7) was moved to Plan 11-03 Task 1 per revision fix Blocker 3 Option A — it must land in Wave 2 before Wave 2's first Vercel deploy. This plan now only covers Sentry `traces_sampler` + docs + ROADMAP + external dashboards.

Output: Hot-path Sentry Performance, 2 live UptimeRobot monitors, `docs/deploy.md` runbook, corrected ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-deployment/11-CONTEXT.md
@.planning/phases/11-deployment/11-RESEARCH.md
@.planning/phases/11-deployment/11-VALIDATION.md
@.planning/phases/11-deployment/11-02-PROVISIONING.md
@backend/main.py
@backend/config.py
@.github/workflows/test.yml
@.github/workflows/smoke.yml
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Sentry hot-path Performance (traces_sampler)</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py (lines 18-21 show current sentry_sdk.init — confirm traces_sampler is NOT yet present)
    - backend/config.py (confirms settings.sentry_dsn field exists, line 45)
    - .planning/phases/11-deployment/11-RESEARCH.md §Code Example 3 (exact traces_sampler pattern)
    - .planning/phases/11-deployment/11-CONTEXT.md §D-14 (5 hot paths enumerated)
    - .planning/phases/11-deployment/11-03-PLAN.md Task 1 (confirms frontend-bundle secret-leak grep already landed in test.yml in Wave 2 — do not duplicate here)
  </read_first>
  <action>
Single edit.

**backend/main.py** — Locate the existing `if settings.sentry_dsn:` block (around line 18-21). Replace with hot-path performance sampling per D-14. Insert this before the current init call:

```python
_HOT_PATHS = {
    "POST /agent/message",
    "POST /jobs/launch",
    "POST /webhooks/runpod",
    "POST /webhooks/heartbeat",
    "POST /jobs/{job_id}/upload-urls",
}


def _traces_sampler(sampling_context: dict) -> float:
    """Sentry traces_sampler per Phase 11 D-14.

    Sample 100% of the 5 hot paths that actually cost money or user-facing
    latency; 0% of everything else to stay within the free-tier quota.
    """
    txn_ctx = sampling_context.get("transaction_context", {}) or {}
    name = txn_ctx.get("name", "") or ""
    return 1.0 if name in _HOT_PATHS else 0.0
```

Then modify the existing `sentry_sdk.init(...)` call. Current form (as read from main.py line 18-21):
```python
if settings.sentry_dsn:
    sentry_sdk.init(
        dsn=settings.sentry_dsn,
        ...existing args...
    )
```

Target form — add `traces_sampler=_traces_sampler` and `environment=` argument:
```python
if settings.sentry_dsn:
    sentry_sdk.init(
        dsn=settings.sentry_dsn,
        traces_sampler=_traces_sampler,
        environment=("production" if not settings.debug else "development"),
        # existing args preserved
        ...
    )
```

Preserve all other existing init args (integrations, etc.) unchanged. Do NOT remove FastAPI/Starlette integrations if present.

Do NOT edit `.github/workflows/test.yml` here — the frontend-bundle secret-leak grep is already added in Plan 11-03 Task 1 per revision fix Blocker 3 Option A. Duplicating would drift.
  </action>
  <acceptance_criteria>
- `grep "traces_sampler" backend/main.py` matches at least 2 times (definition + init arg)
- `grep "_HOT_PATHS" backend/main.py` matches
- `grep "POST /jobs/launch" backend/main.py` matches (from the hot path set)
- `grep "POST /webhooks/runpod" backend/main.py` matches
- `grep "POST /webhooks/heartbeat" backend/main.py` matches
- `grep "POST /jobs/{job_id}/upload-urls" backend/main.py` matches
- `grep "POST /agent/message" backend/main.py` matches
- Backend still imports cleanly: `cd backend && python -c "import main"` exits 0
- `cd backend && pytest -x -k "not e2e"` still passes
- (Inherited from Plan 11-03 Task 1): `grep "SUPABASE_SERVICE_ROLE_KEY" .github/workflows/test.yml` matches
  </acceptance_criteria>
  <verify>
    <automated>grep -q "traces_sampler" backend/main.py && grep -q "POST /jobs/launch" backend/main.py && cd backend && python -c "import main"</automated>
  </verify>
  <done>Sentry Performance samples 100% of the 5 D-14 hot paths and 0% elsewhere. (CI frontend-bundle grep already in test.yml from Plan 11-03 Task 1.)</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/deploy.md (env matrix + rotation runbook + rollback drill) and correct ROADMAP.md SC 6 + SC 8</name>
  <files>docs/deploy.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/11-deployment/11-CONTEXT.md §D-10 (rotation procedure), §D-12 (per-runtime matrix), §ROADMAP Corrections Required
    - .planning/phases/11-deployment/11-RESEARCH.md §Pattern 6 (exact 6-step rotation runbook)
    - .planning/phases/11-deployment/11-RESEARCH.md §Runtime State Inventory (no legacy state to migrate)
    - .planning/ROADMAP.md (lines 218-228 — current Phase 11 SC 6 + SC 8 wording)
    - .env.example (post-Plan 11-04 — source of truth for env matrix rows)
    - scripts/rollback_drill.sh (Plan 11-01 output — referenced from docs)
    - CLAUDE.md project instructions (no emojis in written artifacts)
  </read_first>
  <action>
Two edits.

**Create docs/deploy.md** with three top-level sections: Env matrix, Secret rotation runbook, Rollback drill.

```markdown
# Kendrew Deployment Runbook

Last updated: 2026-04-24 (Phase 11).

This document is the operational source of truth for deploying, rotating
secrets, and rolling back the Kendrew production platform. Update it any time
a deploy concern surfaces that would not be obvious to the next on-call.

## Environments

| Environment | Frontend | Backend | Database | Redis | R2 Bucket | Modal Env |
|-------------|----------|---------|----------|-------|-----------|-----------|
| Production | https://bindwave.com | https://app.bindwave.com | kendrew-prod (Supabase Free; upgrade to Pro before public launch) | kendrew-prod (Upstash, TLS) | kendrew-prod | main |
| Staging | https://staging.bindwave.com | https://app-staging.bindwave.com | kendrew-staging (Supabase Free) | kendrew-staging (Upstash, TLS) | kendrew-staging | staging |
| Local | http://localhost:5173 | http://localhost:8000 | local Supabase CLI :54322 | Docker redis :6379 | MinIO :9000 | n/a |

## Env Variable Matrix (Phase 11 D-12)

Where each env var lives in production. The source of truth is `.env.example`
with `# runtime:` comments; this table is a reading-friendly summary.

| Key | Railway Backend | Railway Worker | Vercel | Modal Secret | Notes |
|-----|:---------------:|:--------------:|:------:|:------------:|-------|
| SUPABASE_URL | Yes | Yes | Yes (as VITE_SUPABASE_URL) | - | Public |
| SUPABASE_ANON_KEY | Yes | Yes | Yes (as VITE_SUPABASE_ANON_KEY) | - | Public |
| SUPABASE_SERVICE_ROLE_KEY | Yes | Yes | **NO — never Vercel** | - | Pitfall 5 |
| SUPABASE_JWT_SECRET | Yes | - | - | - | Backend auth |
| DATABASE_URL | Yes | Yes | - | - | Supavisor transaction pooler, port 6543 |
| S3_ENDPOINT_URL | Yes | Yes | - | kendrew-r2 | R2 account endpoint |
| S3_ACCESS_KEY | Yes | Yes | - | kendrew-r2 | Per-bucket token |
| S3_SECRET_KEY | Yes | Yes | - | kendrew-r2 | Per-bucket token |
| S3_BUCKET_NAME | Yes | Yes | - | kendrew-r2 | kendrew-prod or kendrew-staging |
| REDIS_URL | Yes | Yes | - | - | rediss:// (TLS) |
| CSRF_SECRET | Yes | - | - | - | openssl rand -hex 32 |
| COOKIE_SECURE | Yes (true) | - | - | - | prod only |
| CORS_ORIGINS | Yes | - | - | - | exact-match list |
| DEBUG | Yes (false) | Yes (false) | - | - | prod MUST be false |
| SENTRY_DSN | Yes | Yes | - | - | backend project DSN |
| VITE_SENTRY_DSN_FRONTEND | - | - | Yes | - | frontend project DSN |
| ANTHROPIC_API_KEY | Yes | - | - | - | agent |
| STRIPE_SECRET_KEY | Yes | - | **NO** | - | live key in prod, test in staging |
| STRIPE_WEBHOOK_SECRET | Yes | - | - | - | |
| VITE_STRIPE_PUBLISHABLE_KEY | - | - | Yes | - | public |
| GPU_PROVIDER | Yes (modal) | Yes (modal) | - | - | |
| MODAL_TOKEN_ID | Yes | Yes | - | - | also in GitHub Actions secrets |
| MODAL_TOKEN_SECRET | Yes | Yes | - | - | also in GitHub Actions secrets |
| MODAL_ENVIRONMENT | Yes | Yes | - | - | main or staging |
| RESEND_API_KEY | Yes | Yes | - | - | worker sends completion emails + spend alerts |
| RESEND_FROM_EMAIL | Yes | Yes | - | - | `Kendrew.AI <jobs@bindwave.com>` |
| WEBHOOK_HMAC_SECRET | Yes | - | - | - | Phase 11 D-10 — canonical name; source of truth |
| WEBHOOK_HMAC_SECRET_PREV | Yes | - | - | - | rotation window only |
| RUNPOD_WEBHOOK_SECRET | **NO (backend config.py alias only)** | - | - | - | deprecated alias — backend resolves via model_post_init; NOT set in Railway |
| APP_BASE_URL | Yes | - | - | - | `https://app.bindwave.com` |
| VITE_APP_BASE_URL | - | - | Yes | - | same value |
| GPU_DAILY_SPEND_ALERT_USD | Yes | Yes | - | - | cron threshold (Phase 11 D-16) |

## Secret Rotation Runbook (Webhook HMAC — D-10)

Rotates `WEBHOOK_HMAC_SECRET` without dropping any in-flight webhook. Expect
60-90 min total including propagation wait. Runbook applies to production;
staging follows the same steps against its own Railway service.

1. Generate new secret:
   ```
   openssl rand -hex 32
   ```
2. In Railway → kendrew-backend-prod → Variables:
   - Set `WEBHOOK_HMAC_SECRET_PREV` equal to the CURRENT value of `WEBHOOK_HMAC_SECRET`.
   - Set `WEBHOOK_HMAC_SECRET` equal to the new value from step 1.
   - Save. Railway restarts the backend service automatically.
3. Update Modal secret in both environments:
   ```
   modal secret create --env main kendrew-webhook WEBHOOK_SECRET=<new value>
   modal deploy --env main infrastructure/modal/<each app>_app.py
   ```
4. Wait 60 minutes AND observe Sentry for "Webhook signed with PREV secret" warnings.
   - Warnings expected: in-flight jobs using pre-rotation secret. Count should drop to 0 within 60 min.
   - If warnings persist past 60 min: some Modal function has not restarted with the new secret. Force redeploy that app.
5. Once warnings are zero for 10 consecutive minutes:
   - Railway → Variables → clear `WEBHOOK_HMAC_SECRET_PREV` (set to empty).
6. Record this rotation:
   - Add a dated entry below (Last Rotations table).

### Last Rotations

| Date | Secret | Operator | Notes |
|------|--------|----------|-------|
| 2026-04-24 | WEBHOOK_HMAC_SECRET | initial provisioning (Phase 11) | baseline |

## Rollback Drill (SC 9 — 5-minute rollback)

Target: restore `/health` green within 5 minutes of detecting a regression.

Scripted drill:
```
bash scripts/rollback_drill.sh --dry-run   # safe anytime
bash scripts/rollback_drill.sh              # interactive — will prompt before destructive ops
```

Manual steps (what the script does):
1. Identify current prod deploy SHAs:
   - `railway deployments --service kendrew-backend-prod --limit 2`
   - `vercel ls kendrew --limit 2`
2. Railway rollback:
   - Railway dashboard → kendrew-backend-prod → Deployments → previous deploy → "Redeploy"
   - OR CLI: `railway rollback --service kendrew-backend-prod`
3. Vercel rollback:
   - Vercel dashboard → kendrew project → Deployments → previous deploy → three dots → "Promote to Production"
   - OR CLI: `vercel rollback https://bindwave.com`
4. Verify `/health` returns 200:
   - `curl -sI https://app.bindwave.com/health` should return 200 within 2 minutes of rollback.
5. Record incident in `docs/incidents/YYYY-MM-DD-<slug>.md` (create if it doesn't exist).

### Last Drill

| Date | Target | Wall clock to /health green | Blockers |
|------|--------|-----------------------------|----------|
| TBD | staging | pending | - |

Run the drill at least once against staging before declaring SC 9 green. The
drill does NOT require introducing an intentional break — rolling back to any
prior-known-good deploy proves the < 5-min target.

## Monitoring

| Signal | Tool | Channel |
|--------|------|---------|
| Application errors | Sentry (kendrew-backend, kendrew-frontend) | #kendrew-alerts Slack + email |
| External liveness | UptimeRobot (app.bindwave.com/health, app-staging.bindwave.com/health) | #kendrew-alerts + email |
| Daily GPU spend | arq cron `check_daily_gpu_spend` | Resend email to leo@ranomics.com |
| Post-deploy smoke | GitHub Actions smoke.yml | Sentry + #kendrew-alerts on fail |

Sentry Performance traces sample 100% of the 5 D-14 hot paths:
- `POST /agent/message`
- `POST /jobs/launch`
- `POST /webhooks/runpod`
- `POST /webhooks/heartbeat`
- `POST /jobs/{job_id}/upload-urls`

## Deployment Flow

1. PR opened targeting `main`. GitHub Actions `test.yml` runs 4 gates (backend, frontend, E2E, lint+typecheck+coverage) plus a frontend-bundle secret-leak grep (Phase 11 SC 7). Branch protection blocks merge on any failure.
2. PR touching `infrastructure/modal/**`, `docker/**`, or `backend/pipelines/**` also triggers `deploy-modal.yml` against Modal `staging` env.
3. On merge to `main`:
   - Railway auto-deploys kendrew-backend-prod + kendrew-worker-prod. `railway.toml` runs `supabase db push` as preDeployCommand; failure aborts the deploy.
   - Vercel auto-deploys the frontend to https://bindwave.com.
   - `deploy-modal.yml` deploys all 5 Modal apps to `main` env (if PR touched Modal paths).
   - `smoke.yml` runs after deploy completes (informational — D-08). Failures post to Sentry + #kendrew-alerts. Human decides rollback.

## Subprocessors (Phase 10 reference)

See `frontend/src/content/legal/subprocessors.mdx` — the authoritative list.
Summary: Supabase, Cloudflare (R2 + DNS), Modal, RunPod (quarantined), Stripe,
Anthropic, Resend, Sentry.
```

**Edit .planning/ROADMAP.md** — Read current lines 216-229 (Phase 11 block). Make exactly TWO changes:

1. Replace SC 6 (`GPU jobs dispatch to RunPod from production backend`) with:
   `GPU jobs dispatch to Modal (primary GPU provider per Phase 10) from production backend; RunPod remains quarantined as an emergency fallback only`

2. Replace SC 8 (`Monitoring: Sentry for errors, uptime monitoring with PagerDuty/Opsgenie alerting`) with:
   `Monitoring: Sentry for errors + UptimeRobot for uptime, both routing to #kendrew-alerts Slack (PagerDuty/Opsgenie deferred until a paying customer with an uptime SLO)`

Do NOT modify any other SC or any other phase. Do not reorder. Do not add new bullets.
  </action>
  <acceptance_criteria>
- `test -f docs/deploy.md` (file exists)
- `grep "WEBHOOK_HMAC_SECRET_PREV" docs/deploy.md` matches (rotation runbook documents the variable)
- `grep "openssl rand -hex 32" docs/deploy.md` matches (rotation step 1)
- `grep "rollback_drill.sh" docs/deploy.md` matches (links to Plan 11-01 script)
- `grep "supabase db push" docs/deploy.md` matches (explains predeploy flow)
- `grep "pooler.supabase.com:6543\|Supavisor transaction pooler" docs/deploy.md` matches
- `grep "Pitfall 5" docs/deploy.md` matches (references service-role-in-Vercel prohibition)
- `grep "UptimeRobot" docs/deploy.md` matches
- `grep -c "POST /agent/message\|POST /jobs/launch\|POST /webhooks/runpod\|POST /webhooks/heartbeat\|POST /jobs/{job_id}/upload-urls" docs/deploy.md` returns 5 (all D-14 hot paths listed)
- `grep -i "Modal.*primary GPU provider" .planning/ROADMAP.md` matches (SC 6 correction)
- `grep -i "RunPod.*quarantined" .planning/ROADMAP.md` matches (SC 6 correction)
- `grep "UptimeRobot.*Slack\|Sentry.*UptimeRobot" .planning/ROADMAP.md` matches (SC 8 correction)
- `grep -i "PagerDuty/Opsgenie alerting" .planning/ROADMAP.md` returns 0 matches as an active SC (may still appear in deferred notes/context — but SC 8 MUST be rewritten)
- `grep -i "RunPod from production backend" .planning/ROADMAP.md` returns 0 matches (old SC 6 wording removed)
- docs/deploy.md contains no emojis (per CLAUDE.md): `python -c "import re,sys; sys.exit(bool(re.search(r'[\U0001F300-\U0001F9FF\u2600-\u27BF]', open('docs/deploy.md').read())))"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/deploy.md && grep -q "WEBHOOK_HMAC_SECRET_PREV" docs/deploy.md && grep -q "rollback_drill.sh" docs/deploy.md && grep -qi "Modal.*primary" .planning/ROADMAP.md && grep -qi "UptimeRobot" .planning/ROADMAP.md && ! grep -q "RunPod from production backend" .planning/ROADMAP.md</automated>
  </verify>
  <done>docs/deploy.md is the single operational source of truth for envs, secret rotation, and rollback; ROADMAP.md Phase 11 SC 6 + SC 8 now match CONTEXT decisions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create Sentry projects + UptimeRobot monitors, run synthetic Sentry test, run SC 6 GPU dispatch validation</name>
  <files>(external dashboards + manual validation)</files>
  <read_first>
    - .planning/phases/11-deployment/11-VALIDATION.md §Per-SC Verification Map (SC 6, SC 8, SC 9 rows)
    - .planning/phases/11-deployment/11-02-PROVISIONING.md (need to add SENTRY_DSN + VITE_SENTRY_DSN_FRONTEND after project creation)
    - scripts/validate_prod_gpu.sh (Plan 11-01 — smoke Modal BindCraft)
    - scripts/rollback_drill.sh (Plan 11-01 — 5-min drill)
    - backend/debug_routes.py (Plan 11-01 — /debug/sentry-test)
    - docs/deploy.md (Task 2 output — rollback-drill section)
  </read_first>
  <action>
Human-executed monitoring setup and validation. Claude has already wired backend/main.py traces_sampler, docs/deploy.md, and ROADMAP corrections (Tasks 1 + 2). The frontend-bundle secret-leak CI grep is already in test.yml from Plan 11-03 Task 1.

Leo now runs 6 blocks: (A) create Sentry kendrew-backend + kendrew-frontend projects, paste DSNs into Railway + Vercel, add Slack #kendrew-alerts integration; (B) validate SC 8 by curl-ing https://app-staging.bindwave.com/debug/sentry-test and confirming Sentry + Slack receive the synthetic ZeroDivisionError; (C) create 2 UptimeRobot HTTPS monitors at 5-min intervals on app.bindwave.com/health + app-staging.bindwave.com/health; (D) validate SC 6 by running `bash scripts/validate_prod_gpu.sh staging` with Modal credentials configured via `modal token set` (not shell-exported — shell history risk); (E) validate SC 9 via a **safe rollback to a prior known-good deploy** (no intentional breakage required — SC 9 only asks for a < 5-min rollback, and rolling back to any recent prior deploy proves it); (F) optionally paste Sentry DSNs into PROVISIONING.md for reference. See <how-to-verify> below for detailed steps per block.
  </action>
  <what-built>
Claude has wired Sentry traces_sampler and hot-path init in backend/main.py, written docs/deploy.md, and corrected ROADMAP.md SC 6 + SC 8. The /debug/sentry-test route from Plan 11-01 is live in staging (DEBUG=true). The rollback_drill.sh script is ready. The CI frontend-bundle grep landed in Plan 11-03 Task 1 (Wave 2).

All that remains is clicking through Sentry + UptimeRobot dashboards, pasting the resulting DSNs into Railway + Vercel, running the 3 validation checks (synthetic Sentry error → Slack; UptimeRobot pings green; Modal BindCraft smoke completes), and running one safe rollback drill on staging to record the wall-clock.
  </what-built>
  <how-to-verify>
**Block A — Sentry projects + Slack routing (expected ~15 min):**
1. sentry.io → New Project → Platform: Python (FastAPI) → Name: kendrew-backend. Copy DSN.
2. sentry.io → New Project → Platform: React → Name: kendrew-frontend. Copy DSN.
3. Paste `SENTRY_DSN` into Railway Variables for both backend-prod and backend-staging (different DSN for prod vs staging if separate projects are created; acceptable to share one project with `environment=` tag differing).
4. Paste `VITE_SENTRY_DSN_FRONTEND` into Vercel → Production + Preview env vars.
5. Sentry → Settings → Integrations → Slack → add #kendrew-alerts as alert destination for both projects.
6. Trigger Railway + Vercel redeploys to pick up the new env vars.

**Block B — Synthetic error test (SC 8 validation):**
1. Against staging (DEBUG=true): `curl -i https://app-staging.bindwave.com/debug/sentry-test` — expect 500.
2. Within 1 min: Sentry dashboard → kendrew-backend → Issues should show a `ZeroDivisionError` from `debug_routes.py`.
3. Within 2 min: #kendrew-alerts Slack channel should receive the Sentry alert post.
4. Hit the hot path `POST /jobs/launch` on staging (use an existing staging test account; launch a draft job). Within 1 min, Sentry → Performance tab should show a transaction for `POST /jobs/launch` with traces_sample_rate=1.0.

**Block C — UptimeRobot monitors (SC 8 validation):**
1. uptimerobot.com → Add Monitor → HTTPS → `https://app.bindwave.com/health` → 5-min interval → alert contacts: Slack webhook + email.
2. Same for `https://app-staging.bindwave.com/health`.
3. Wait 10 min. Both monitors should show "Up" status.

**Block D — SC 6 Modal GPU dispatch smoke:**

Configure Modal CLI credentials without leaving tokens in shell history. Prefer the Modal-native token store:

```
modal token set --profile kendrew
# When prompted, paste MODAL_TOKEN_ID + MODAL_TOKEN_SECRET from PROVISIONING.md.
# Confirm profile:
modal profile current
```

Alternative (if Leo prefers per-project env): create `.envrc` in the repo root (already in `.gitignore` — confirm before writing), use `direnv` to load it, and add the two vars there. Never `export MODAL_TOKEN_SECRET=...` at a bash prompt — it lands in `~/.bash_history` permanently.

Then run:
```
bash scripts/validate_prod_gpu.sh staging
```
Expect exit code 0 and "PASS: Modal smoke run completed in env=staging".

**Block E — SC 9 rollback drill on staging (safe, no intentional breakage):**

SC 9 requires "rollback possible within 5 minutes". Rolling back to any prior-known-good staging deploy proves this. Do NOT deploy an intentional break — staging auto-deploys from main (D-05) and an intentional break would push broken code into the normal deploy flow, which defeats the purpose and risks contaminating the default deploy branch routing.

1. Identify the previous staging deploy SHA:
   ```
   railway deployments list --service kendrew-backend-staging --limit 2 --json
   ```
   Take the second entry's `id` (the prior known-good deploy).

2. Roll back staging backend to that SHA:
   ```
   railway rollback --service kendrew-backend-staging --deployment <id>
   ```

3. Start a wall-clock timer at the moment you issue the command. Poll `/health` until 200:
   ```
   while :; do
     code=$(curl -s -o /dev/null -w "%{http_code}" https://app-staging.bindwave.com/health)
     echo "$(date +%T) /health=$code"
     [ "$code" = "200" ] && break
     sleep 5
   done
   ```
   Target: under 5 minutes.

4. Document the recorded time in docs/deploy.md "Last Drill" row (replace `TBD` / `pending` with date + seconds).

5. Optional: re-deploy current main to staging (`git push` any no-op commit, or `railway redeploy --service kendrew-backend-staging`) to get back to the latest code. Railway auto-deploy from main handles this.

No intentional breakage is required; rolling back to any prior-known-good deploy proves SC 9.

**Block F — Update PROVISIONING.md (optional housekeeping):**
Paste SENTRY_DSN + VITE_SENTRY_DSN_FRONTEND into PROVISIONING.md (still gitignored) for reference; Railway/Vercel Variables are the true source.
  </how-to-verify>
  <resume-signal>
Type "validated" and paste:
- Sentry `kendrew-backend` issue URL for the synthetic ZeroDivisionError
- UptimeRobot monitor list (both "Up")
- `scripts/validate_prod_gpu.sh staging` stdout showing "PASS"
- Wall-clock seconds from Block E rollback drill (target < 300s)
- `#kendrew-alerts` message screenshot or paste confirming Sentry + UptimeRobot posts arrived

If any block fails, describe the blocker so a gap-closure plan can be scoped.
  </resume-signal>
  <acceptance_criteria>
- Sentry projects kendrew-backend + kendrew-frontend exist
- SENTRY_DSN is set in Railway backend-prod + backend-staging Variables
- VITE_SENTRY_DSN_FRONTEND is set in Vercel Production + Preview env vars
- UptimeRobot shows both monitors "Up" for at least 10 min
- Synthetic ZeroDivisionError visible in Sentry within 1 min of curl
- `#kendrew-alerts` Slack channel received at least one Sentry post from the synthetic error
- `bash scripts/validate_prod_gpu.sh staging` exits 0 (Modal token was set via `modal token set`, not `export` in shell — confirm `history | grep MODAL_TOKEN_SECRET` returns 0 lines)
- Rollback drill on staging completed via `railway rollback --deployment <prior-sha>` with wall-clock recorded in docs/deploy.md "Last Drill" row (target < 5 min)
- No intentional breakage branch (e.g., `staging-broken`) was pushed
- Sentry Performance dashboard shows a transaction for one of the 5 hot paths (e.g., `POST /jobs/launch`)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Last Drill" docs/deploy.md && grep -qE "[0-9]+s|[0-9]+ seconds" docs/deploy.md || echo "MANUAL: rollback drill wall-clock not yet recorded in docs/deploy.md"</automated>
  </verify>
  <done>Sentry live with hot-path Performance; UptimeRobot 5-min monitors green for both envs; synthetic error routed to Slack; SC 6 Modal smoke passes with non-history token config; SC 9 rollback drill documented via safe prior-deploy rollback (no intentional breakage).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend exception handler → Sentry | Stack traces transmitted (may contain PII if before_send not configured) |
| UptimeRobot → /health | External polling from a public IP range — must not expose secrets in /health response |
| Railway deploy webhook → Slack | Sentry + smoke workflow post to public-ish channel |
| docs/deploy.md → future engineer | Rotation runbook is safety-critical; errors here cause auth bypass or outage |
| Leo's shell history → Modal tokens | `export MODAL_TOKEN_SECRET=...` leaks to ~/.bash_history permanently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | Sentry transmitting PII in stack traces (CONTEXT seed) | mitigate | `sentry_sdk.init(send_default_pii=False)` default; confirm with `grep send_default_pii backend/main.py` — if absent, set to False explicitly; docs/deploy.md notes this |
| T-11-05-02 | Information Disclosure | /health leaking secrets in response body | accept | Existing /health returns only status booleans per Phase 5 D-3B Layer 2; unchanged this phase |
| T-11-05-03 | Spoofing | UptimeRobot monitor URL visible in publicly-posted alerts → attacker could DoS /health | mitigate | /health already behind slowapi global rate limit (60/min per IP) from Phase 5; UptimeRobot monitors from a distinct IP pool, no override |
| T-11-05-04 | Tampering | Rotation runbook skipped/forgotten (CONTEXT seed) | mitigate | docs/deploy.md has "Last Rotations" table; quarterly calendar reminder (manual) |
| T-11-05-05 | Information Disclosure | Slack webhook URL (#kendrew-alerts) committed to repo | mitigate | Slack webhook is configured in Sentry + UptimeRobot + smoke.yml as a GitHub secret, never as a literal URL in repo |
| T-11-05-06 | Information Disclosure | ROADMAP.md editing accidentally leaks implementation secrets | accept | ROADMAP.md is SC-level prose, never contains secret values |
| T-11-05-07 | DoS | /debug/sentry-test reachable in prod (DEBUG=true) | mitigate | Plan 11-03 Block B enumerates DEBUG=false for prod Railway Variables; Plan 11-01 route returns 404 when debug=false |
| T-11-05-08 | Tampering | Sentry traces_sampler misconfigured → 100% of all requests sampled, blows free-tier quota | mitigate | _traces_sampler returns 0.0 for any txn name NOT in _HOT_PATHS; explicit allowlist, not denylist |
| T-11-05-09 | Information Disclosure | MODAL_TOKEN_SECRET persisted in Leo's ~/.bash_history via `export` | mitigate | Block D instructs `modal token set --profile kendrew` (Modal-native keychain-style store) OR `.envrc` + `direnv` (gitignored); explicit acceptance check greps history for leaked token names |
| T-11-05-10 | Tampering | Intentional staging breakage auto-deploys to staging and contaminates deploy flow (original Block E design) | mitigate | Block E rewritten to roll back to a prior known-good SHA only; no `staging-broken` branch created |
</threat_model>

<verification>
- `grep -q traces_sampler backend/main.py`
- `test -f docs/deploy.md`
- `grep -qi "Modal.*primary" .planning/ROADMAP.md` (SC 6 corrected)
- `grep -qi "UptimeRobot" .planning/ROADMAP.md` (SC 8 corrected)
- UptimeRobot dashboard shows 2 "Up" monitors
- Sentry receives synthetic error when /debug/sentry-test hit on staging
- bash scripts/validate_prod_gpu.sh staging exits 0
</verification>

<success_criteria>
All 9 Phase 11 success criteria (as amended by CONTEXT) are verifiable:
- SC 1: `curl -sI https://bindwave.com` returns 200 with HSTS (Plan 11-03)
- SC 2: Railway auto-deploys Docker from main; `curl -sf https://app.bindwave.com/health` returns 200 (Plans 11-03, 11-04)
- SC 3: Supabase Free with daily backup (7-day retention); asyncpg Supavisor-safe; predeploy migration runs; PITR deferred to Pro upgrade before public launch (Plans 11-02, 11-03, 11-04)
- SC 4: REDIS_URL uses rediss:// TLS (Plans 11-02, 11-03)
- SC 5: R2 buckets per env; presigned URLs work (Plan 11-02)
- SC 6 (corrected): Modal primary, RunPod quarantined (Plan 11-03 deploy-modal.yml flip; ROADMAP correction this plan)
- SC 7: Platform-native secrets; no service_role key in Vercel bundle (Plan 11-03 Block C + CI grep in Plan 11-03 Task 1)
- SC 8 (corrected): Sentry + UptimeRobot + Slack (Sentry wiring + UptimeRobot monitors this plan)
- SC 9: Rollback drill < 5 min (scripts/rollback_drill.sh + safe prior-deploy rollback + docs/deploy.md Last Drill recording this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/11-deployment/11-05-SUMMARY.md`.

Summary file contains no emoji characters (verify: `python -c "import re,sys; sys.exit(bool(re.search(r'[\U0001F300-\U0001F9FF\u2600-\u27BF]', open('.planning/phases/11-deployment/11-05-SUMMARY.md').read())))"` exits 0).
</output>
