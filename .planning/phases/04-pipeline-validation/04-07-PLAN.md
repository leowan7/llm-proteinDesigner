---
phase: 04-pipeline-validation
plan: 07
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-05", "04-06"]
files_modified:
  - backend/webhooks/router.py
  - backend/pipelines/base.py
autonomous: false
requirements: [PIPE-06]
must_haves:
  truths:
    - "Webhook handler uses tool-specific result parser to normalize RunPod output into CandidateResult list"
    - "Each tool's output is correctly parsed with the right score fields"
    - "Results page displays candidates with tool-appropriate metric labels"
    - "Output PDBs are downloadable from R2 via presigned GET URLs from the results page"
    - "Metrics CSV is accessible from the results page"
  artifacts:
    - path: "backend/webhooks/router.py"
      provides: "Updated webhook handler that uses PIPELINE_MAP for result parsing"
      contains: "parse_results"
  key_links:
    - from: "backend/webhooks/router.py"
      to: "backend/pipelines/*.py"
      via: "PIPELINE_MAP[tool].parse_results(output)"
      pattern: "PIPELINE_MAP.*parse_results"
    - from: "backend/webhooks/router.py"
      to: "backend/jobs/models.py"
      via: "JobResult with CandidateResult list"
      pattern: "JobResult"
---

<objective>
Wire the result parsing pipeline into the webhook handler and verify end-to-end: RunPod webhook fires, handler parses tool-specific output using PIPELINE_MAP, stores normalized CandidateResult in the database, and results display correctly on the frontend job page.

Purpose: The final integration step that closes the loop. All 5 Docker images produce tool-specific output formats. This plan ensures the webhook handler correctly normalizes all of them using the parsers built in 04-01, and that the frontend can display the results.

Output: Updated webhook handler with tool-aware result parsing. End-to-end verification across all 5 tools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pipeline-validation/04-01-SUMMARY.md
@.planning/phases/04-pipeline-validation/04-02-SUMMARY.md
@.planning/phases/04-pipeline-validation/04-03-SUMMARY.md
@.planning/phases/04-pipeline-validation/04-04-SUMMARY.md
@.planning/phases/04-pipeline-validation/04-05-SUMMARY.md
@.planning/phases/04-pipeline-validation/04-06-SUMMARY.md

@backend/webhooks/router.py
@backend/jobs/models.py
@backend/pipelines/base.py

<interfaces>
From backend/pipelines/__init__.py (created in 04-01):
```python
PIPELINE_MAP: dict[str, ToolPipeline] = {
    "rfdiffusion": RFdiffusionPipeline(),
    "bindcraft": BindCraftPipeline(),
    "rfantibody": RFantibodyPipeline(),
    "boltzgen": BoltzGenPipeline(),
    "pxdesign": PXDesignPipeline(),
}
```

From backend/pipelines/base.py:
```python
class ToolPipeline(ABC):
    def parse_results(self, output: dict) -> list[CandidateResult]: ...
```

From backend/jobs/models.py:
```python
class CandidateResult(BaseModel):
    rank: int
    pdb_key: str
    scores: dict

class JobResult(BaseModel):
    candidate_count: int
    candidates: list[CandidateResult]
    next_steps: str
    zero_output: bool = False
    gpu_seconds: int
    gpu_cost_usd: float
    runtime_minutes: int
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire result parsers into webhook handler and verify full pipeline</name>
  <files>backend/webhooks/router.py</files>
  <action>
Update the webhook handler (`backend/webhooks/router.py`) to use the tool-specific result parsers from PIPELINE_MAP:

1. In the webhook completion handler (where RunPod sends COMPLETED status):
   - Extract the `tool` from the job record's `job_spec` column
   - Look up the pipeline: `from pipelines import PIPELINE_MAP; pipeline = PIPELINE_MAP[tool]`
   - Parse results: `candidates = pipeline.parse_results(webhook_output)` where `webhook_output` is the `output` field from the RunPod webhook payload
   - Build `JobResult` with the parsed candidates, candidate_count, next_steps from the output, and billing data (gpu_seconds, gpu_cost_usd, runtime_minutes)
   - Handle `zero_output` case: if candidates list is empty and tool is "bindcraft", set `zero_output=True`
   - Store the `JobResult` as JSONB in the jobs.results column

2. Ensure the webhook handler handles all 5 tools — no hardcoded tool-specific logic in the router itself. All tool-specific behavior lives in the pipeline parsers.

3. Add error handling for parse_results failures — if parsing fails (unexpected output format), log the raw output, mark the job as failed with error_category="result_parse_error", and include the raw output in the error message for debugging.

4. Verify the existing frontend `JobPage` and result components can display the CandidateResult.scores dict for all 5 tools. The scores dict keys differ per tool:
   - RFdiffusion: ipTM, pLDDT, i_pAE
   - BindCraft: ipTM, pLDDT, RMSD, shape_complementarity, SAP
   - RFantibody: rf2_confidence, cdr_geometry
   - BoltzGen: refolding_rmsd, ipTM, pLDDT
   - PXDesign: ipTM, pLDDT, pAE, filter_status
   The frontend should render whatever keys are in the scores dict (generic key-value display). No tool-specific frontend changes needed if the score display is already generic.
  </action>
  <verify>
    <automated>cd c:/Users/lab/Documents/Claude_projects/llm-proteinDesigner && python -c "
from backend.pipelines import PIPELINE_MAP
from backend.jobs.models import CandidateResult
# Verify all parsers work with mock output
for tool, pipeline in PIPELINE_MAP.items():
    mock_output = {'candidates': [{'rank': 1, 'pdb_key': 'design_001.pdb', 'scores': {'ipTM': 0.85}}], 'candidate_count': 1, 'next_steps': 'test'}
    candidates = pipeline.parse_results(mock_output)
    assert len(candidates) == 1
    assert isinstance(candidates[0], CandidateResult)
print('OK: All 5 result parsers work')
"</automated>
  </verify>
  <done>Webhook handler uses PIPELINE_MAP[tool].parse_results() to normalize all 5 tools' output into CandidateResult. Zero-output handled for BindCraft. Parse errors caught and logged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full end-to-end pipeline: agent -> job dispatch -> RunPod GPU execution -> webhook -> result parsing -> frontend display. For all 5 tools.</what-built>
  <how-to-verify>
    1. For each tool that has a completed pilot run (from plans 04-02 through 04-06):
       a. Navigate to the job's page in the Kendrew frontend
       b. Verify the job status shows "Complete"
       c. Verify candidates are displayed with ranked scores
       d. Verify score labels are appropriate for the tool (ipTM for RFdiffusion, shape_complementarity for BindCraft, etc.)
       e. Click a candidate PDB download link — verify the file downloads and is a valid PDB
       f. Check the metrics CSV download link works
    2. If any tool's pilot is still pending, note which ones are verified
    3. Verify that the BindCraft zero-output case displays the BindCraftZeroOutputCard (if applicable)
  </how-to-verify>
  <resume-signal>Type "approved" with which tools are verified, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
1. Webhook handler uses PIPELINE_MAP for all result parsing (no hardcoded tool logic)
2. All 5 tools' outputs parse into CandidateResult without errors
3. Frontend displays candidates with correct tool-specific score labels
4. PDB files downloadable from results page
5. Metrics CSV accessible from results page
</verification>

<success_criteria>
- Each tool's output files correctly uploaded to R2, parsed into candidate cards, and displayed on the job results page
- Webhook handler handles all 5 tools generically via PIPELINE_MAP
- Results are downloadable (PDB files + metrics CSV)
- Zero-output case handled correctly for BindCraft
</success_criteria>

<output>
After completion, create `.planning/phases/04-pipeline-validation/04-07-SUMMARY.md`
</output>
