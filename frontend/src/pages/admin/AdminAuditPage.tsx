/**
 * AdminAuditPage — placeholder stub.
 *
 * This component is a minimal stub so App.tsx compiles.
 * Plan 07-04 replaces this with the full audit log table.
 */

export function AdminAuditPage() {
  return <div className="text-muted-foreground text-sm">Audit — coming soon</div>;
}
