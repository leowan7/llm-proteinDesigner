/**
 * AdminRevenuePage — placeholder stub.
 *
 * This component is a minimal stub so App.tsx compiles.
 * Plan 07-04 replaces this with the full revenue chart and breakdown table.
 */

export function AdminRevenuePage() {
  return <div className="text-muted-foreground text-sm">Revenue — coming soon</div>;
}
