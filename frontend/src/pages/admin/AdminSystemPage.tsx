/**
 * AdminSystemPage — placeholder stub.
 *
 * This component is a minimal stub so App.tsx compiles.
 * Plan 07-04 replaces this with the full system health dashboard.
 */

export function AdminSystemPage() {
  return <div className="text-muted-foreground text-sm">System — coming soon</div>;
}
